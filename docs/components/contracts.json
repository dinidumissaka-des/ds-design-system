[
  {
    "name": "avatar",
    "title": "Avatar",
    "description": "A person or entity's image, falling back to initials when there is none.",
    "family": "content",
    "tier": "free",
    "status": {
      "css": {
        "state": "latest",
        "version": "0.1.0"
      },
      "react": {
        "state": "latest",
        "version": "0.1.0"
      },
      "figma": {
        "state": "future"
      }
    },
    "dependencies": [],
    "importPath": "@rata/react",
    "mode": "documented",
    "note": null,
    "extends": "Omit<ImgHTMLAttributes<HTMLImageElement>, \"src\" | \"alt\" | \"size\">",
    "props": [
      {
        "name": "name",
        "optional": false,
        "type": "string",
        "description": "The person or entity's name: both the fallback initials and the accessible name.",
        "declared": true,
        "summary": "The person or entity's name. Required — it is both the fallback initials and the accessible name.",
        "use": [
          "Always. The name is what makes the fallback possible and the image describable."
        ],
        "dont": [
          "Passing an email or a user id. Initials derived from `a.hartley@` are not initials, and the accessible name would be an address."
        ],
        "conflicts": [],
        "a11y": "Used as the image's `alt` when `decorative` is not set. When the image is missing, the initials are shown but the full name is still what is announced — a screen reader should never hear 'AH'."
      },
      {
        "name": "src",
        "optional": true,
        "type": "string",
        "description": "Image URL. Omit, or let it fail, to fall back to initials.",
        "declared": true,
        "summary": "Image URL. Omit, or let it fail, to fall back to initials.",
        "use": [
          "A real photo or logo. The fallback is automatic, so there is no need to branch on whether one exists."
        ],
        "dont": [],
        "conflicts": [],
        "a11y": null
      },
      {
        "name": "size",
        "optional": true,
        "type": "AvatarSize",
        "description": "Diameter, from the control scale so an avatar lines up with the controls beside it.",
        "default": "\"md\"",
        "values": [
          "sm",
          "md",
          "lg"
        ],
        "declared": true,
        "summary": "Diameter, from the `size.control.*` scale so an avatar lines up with the controls beside it.",
        "use": [],
        "dont": [
          "Mixing sizes within one stack of avatars — they read as a set."
        ],
        "conflicts": [],
        "a11y": null
      },
      {
        "name": "decorative",
        "optional": true,
        "type": "boolean",
        "description": "Marks the avatar as redundant to a name already on screen.",
        "declared": true,
        "summary": "Marks the avatar as redundant to a name already on screen.",
        "use": [
          "An avatar immediately beside the person's name in a row — announcing it again is noise."
        ],
        "dont": [
          "Anything standing alone. Without adjacent text the avatar is the only thing identifying the person, so hiding it removes that identification entirely."
        ],
        "conflicts": [],
        "a11y": "Renders `aria-hidden` with an empty `alt`. The `name` is still required, because it still produces the initials."
      },
      {
        "name": "className",
        "optional": true,
        "type": "string",
        "description": null,
        "declared": true,
        "summary": "Class for the avatar's wrapper.",
        "use": [
          "Placing the component in its surrounding layout — a span, a width, a margin the parent cannot express."
        ],
        "dont": [
          "Overriding the size — the three steps are the scale, and a fourth written in a component's CSS is the drift the scale exists to prevent.",
          "Restyling the component's internals through descendant selectors. Those are the approved token mapping, and overriding them here puts a second answer somewhere the contract cannot see."
        ],
        "conflicts": [],
        "a11y": null
      }
    ],
    "usage": [
      {
        "case": "A photo with its name beside it",
        "when": "A row in a member list, where the name is already text.",
        "example": "<Avatar name=\"Ada Hartley\" src={user.photo} decorative />\n<span>Ada Hartley</span>",
        "notes": "`decorative` because the name is right there — announcing it twice is noise. Note `name` is still required: it is what produces the initials if the photo fails to load."
      },
      {
        "case": "Standing alone",
        "when": "An avatar in a toolbar or a stack, with no adjacent name.",
        "example": "<Avatar name=\"Ada Hartley\" src={user.photo} />",
        "notes": "Not decorative: the avatar is the only thing identifying this person, so it carries the name."
      },
      {
        "case": "No image at all",
        "when": "A user who has not uploaded a photo — which is most of them.",
        "example": "<Avatar name=\"Ada Hartley\" />",
        "notes": "Initials are shown and the full name is announced. There is nothing to branch on: omitting `src`, and an `src` that 404s, land in the same place."
      }
    ],
    "behavior": null,
    "tokens": {
      "base": {
        "summary": "A circular image, falling back to initials on the most recessed surface.",
        "tokens": {
          "size": "size.control.md, with the sm and lg steps at the other sizes — an avatar lines up with the controls beside it, which is the whole reason it borrows the control scale",
          "border-radius": "radius.pill",
          "edge": "border.default solid theme.border.strong — the circle needs its own boundary or the fallback disappears against any container using theme.bg.muted, which is the tone the fallback itself takes. theme.border.default is not an option: composited over a mid surface it measures around 1.2:1",
          "fallback background": "theme.bg.muted — the most recessed surface, so initials read as a placeholder rather than a filled badge",
          "fallback color": "theme.fg.secondary — the pairing the muted background documents for text; theme.fg.muted falls to AA-large there",
          "fallback font-size": "type.supporting.size",
          "fallback font-weight": "type.control.weight",
          "image fit": "cover, so a non-square photo is cropped rather than squashed — a layout fact, not a token"
        }
      }
    },
    "example": "<Avatar name=\"Ada Hartley\" />"
  },
  {
    "name": "badge",
    "title": "Badge",
    "description": "A small inline label for a status, a count, or a category.",
    "family": "content",
    "tier": "free",
    "status": {
      "css": {
        "state": "latest",
        "version": "0.1.0"
      },
      "react": {
        "state": "latest",
        "version": "0.1.0"
      },
      "figma": {
        "state": "future"
      }
    },
    "dependencies": [],
    "importPath": "@rata/react",
    "mode": "documented",
    "note": null,
    "extends": "HTMLAttributes<HTMLSpanElement>",
    "props": [
      {
        "name": "children",
        "optional": false,
        "type": "ReactNode",
        "description": "The badge's text.",
        "declared": true,
        "summary": "The badge's text.",
        "use": [],
        "dont": [
          "A sentence. A badge that wraps is a Notice wearing the wrong component."
        ],
        "conflicts": [],
        "a11y": null
      },
      {
        "name": "variant",
        "optional": true,
        "type": "BadgeVariant",
        "description": "Which role's tone the badge wears.",
        "default": "\"neutral\"",
        "values": [
          "neutral",
          "accent",
          "success",
          "warning",
          "danger"
        ],
        "declared": true,
        "summary": "Which role's tone the badge wears.",
        "use": [
          "`neutral` for a count or a category — the default, and right whenever the badge is not reporting health.",
          "`success` / `warning` / `danger` only for actual status, where the colour is telling the reader something the text also says."
        ],
        "dont": [
          "Colour as decoration. Five badges in five colours that mean nothing teaches readers to ignore the colour on the one that does.",
          "`danger` for 'important'. It means something is wrong."
        ],
        "conflicts": [],
        "a11y": "Every variant uses its role's subtle background with that role's foreground — never the saturated `bg`, which is a non-text indicator tone. The text carries the meaning; colour only reinforces it, so a badge is never colour-only."
      },
      {
        "name": "dot",
        "optional": true,
        "type": "boolean",
        "description": "Renders a status dot before the text.",
        "declared": true,
        "summary": "Renders a status dot before the text.",
        "use": [
          "A status badge in a dense table, where the dot is findable at a glance and the text confirms it."
        ],
        "dont": [
          "A dot with no text. A bare coloured dot is colour-only information, which is exactly what the variant contract forbids."
        ],
        "conflicts": [],
        "a11y": "Decorative and `aria-hidden` — it repeats what the text already says."
      }
    ],
    "usage": [
      {
        "case": "A count beside a label",
        "when": "The commonest case: a number qualifying something next to it.",
        "example": "<Button>\n  Messages\n  <Badge>3</Badge>\n</Button>",
        "notes": "Neutral by default: a count is not a status, and colouring it would imply something about the number."
      },
      {
        "case": "A status in a table row",
        "when": "Reporting health, where the colour reinforces text that already says it.",
        "example": "<Badge variant=\"danger\" dot>Failed</Badge>",
        "notes": "The dot is decorative and aria-hidden. Removing it would lose nothing for a screen reader, which is the test of whether a visual flourish is safe."
      },
      {
        "case": "Reaching for the wrong component",
        "when": "The text is a sentence explaining something.",
        "example": "<Badge variant=\"warning\">Your trial ends in three days, renew to keep your data</Badge>",
        "notes": "Don't. That is a Notice: it has room for the sentence, a heading, and somewhere to put the action. A badge that wraps onto two lines has already stopped being a badge."
      }
    ],
    "behavior": null,
    "tokens": {
      "neutral": {
        "summary": "The default: a count or a category, saying nothing about health.",
        "tokens": {
          "background": "theme.bg.muted",
          "color": "theme.fg.secondary — verified on that background at AA",
          "block-size": "size.icon.lg — a badge is glyph-height, not control-height; it sits inside a line of text rather than beside a button",
          "padding-inline": "space.gap.sm",
          "border-radius": "radius.pill",
          "font-size": "type.supporting.size",
          "font-weight": "type.control.weight — heavier than the text around it, because a badge has to be found before it is read",
          "gap between dot and text": "space.gap.xs",
          "dot size": "space.gap.sm — a dot is a mark, not a glyph, so it takes a spacing step rather than the icon scale"
        }
      },
      "accent": {
        "summary": "A category the brand wants noticed, still not a status.",
        "tokens": {
          "background": "theme.accent-role.subtle",
          "color": "theme.accent-role.fg — a verified pairing on that tint",
          "block-size": "size.icon.lg — a badge is glyph-height, not control-height; it sits inside a line of text rather than beside a button",
          "padding-inline": "space.gap.sm",
          "border-radius": "radius.pill",
          "font-size": "type.supporting.size",
          "font-weight": "type.control.weight — heavier than the text around it, because a badge has to be found before it is read",
          "gap between dot and text": "space.gap.xs",
          "dot size": "space.gap.sm — a dot is a mark, not a glyph, so it takes a spacing step rather than the icon scale"
        }
      },
      "success": {
        "summary": "Healthy, passing, complete.",
        "tokens": {
          "background": "theme.success-role.subtle",
          "color": "theme.success-role.fg — never theme.success-role.bg, which is a non-text indicator tone that fails contrast under white",
          "block-size": "size.icon.lg — a badge is glyph-height, not control-height; it sits inside a line of text rather than beside a button",
          "padding-inline": "space.gap.sm",
          "border-radius": "radius.pill",
          "font-size": "type.supporting.size",
          "font-weight": "type.control.weight — heavier than the text around it, because a badge has to be found before it is read",
          "gap between dot and text": "space.gap.xs",
          "dot size": "space.gap.sm — a dot is a mark, not a glyph, so it takes a spacing step rather than the icon scale"
        }
      },
      "warning": {
        "summary": "Degraded, expiring, needs attention but not broken.",
        "tokens": {
          "background": "theme.warning-role.subtle",
          "color": "theme.warning-role.fg — same reason as success: the saturated bg carries no text",
          "block-size": "size.icon.lg — a badge is glyph-height, not control-height; it sits inside a line of text rather than beside a button",
          "padding-inline": "space.gap.sm",
          "border-radius": "radius.pill",
          "font-size": "type.supporting.size",
          "font-weight": "type.control.weight — heavier than the text around it, because a badge has to be found before it is read",
          "gap between dot and text": "space.gap.xs",
          "dot size": "space.gap.sm — a dot is a mark, not a glyph, so it takes a spacing step rather than the icon scale"
        }
      },
      "danger": {
        "summary": "Failed, revoked, blocked.",
        "tokens": {
          "background": "theme.danger-role.subtle",
          "color": "theme.danger-role.fg",
          "block-size": "size.icon.lg — a badge is glyph-height, not control-height; it sits inside a line of text rather than beside a button",
          "padding-inline": "space.gap.sm",
          "border-radius": "radius.pill",
          "font-size": "type.supporting.size",
          "font-weight": "type.control.weight — heavier than the text around it, because a badge has to be found before it is read",
          "gap between dot and text": "space.gap.xs",
          "dot size": "space.gap.sm — a dot is a mark, not a glyph, so it takes a spacing step rather than the icon scale"
        }
      }
    },
    "example": "<Badge>New</Badge>"
  },
  {
    "name": "breadcrumbs",
    "title": "Breadcrumbs",
    "description": "The trail from the site's root to the current page.",
    "family": "navigation",
    "tier": "free",
    "status": {
      "css": {
        "state": "latest",
        "version": "0.1.0"
      },
      "react": {
        "state": "latest",
        "version": "0.1.0"
      },
      "figma": {
        "state": "future"
      }
    },
    "dependencies": [],
    "importPath": "@rata/react",
    "mode": "documented",
    "note": null,
    "extends": "Omit<HTMLAttributes<HTMLElement>, \"aria-label\">",
    "props": [
      {
        "name": "items",
        "optional": false,
        "type": "BreadcrumbsItem[]",
        "description": "The trail in order, root first. The last item is the current page.",
        "declared": true,
        "summary": "The trail in order, root first. The last item is the current page and is not a link.",
        "use": [
          "Every ancestor of the current page, ending with the page itself."
        ],
        "dont": [
          "Omitting the current page. The trail ends at where you are; ending it at the parent leaves the reader unsure whether the last crumb is a link they are already on.",
          "A trail that is not a hierarchy. Breadcrumbs say where this page sits, not how the user got here — history is the back button's job.",
          "Giving the last item an `href`. It is the page you are on, and this component drops it."
        ],
        "conflicts": [],
        "a11y": "Rendered as an ordered list inside a `<nav>`, so the count and the order are announced. The last item gets `aria-current=\"page\"` and renders as text rather than a link."
      },
      {
        "name": "label",
        "optional": true,
        "type": "string",
        "description": "Accessible name for the navigation landmark.",
        "default": "\"Breadcrumb\"",
        "declared": true,
        "summary": "Accessible name for the navigation landmark.",
        "use": [
          "Changing it only when a page has more than one navigation landmark and they need telling apart."
        ],
        "dont": [],
        "conflicts": [],
        "a11y": "Becomes the `<nav>`'s `aria-label`. A page with several landmarks needs distinct names or they are indistinguishable in a landmark list."
      },
      {
        "name": "separator",
        "optional": true,
        "type": "ReactNode",
        "description": "What sits between crumbs.",
        "default": "\"/\"",
        "declared": true,
        "summary": "What sits between crumbs.",
        "use": [],
        "dont": [
          "A character that carries meaning, such as an arrow implying a flow. The separator is punctuation."
        ],
        "conflicts": [],
        "a11y": "Decorative and `aria-hidden`: the list structure already conveys the order, and announcing 'slash' between every crumb is noise."
      }
    ],
    "usage": [
      {
        "case": "A page three levels deep",
        "when": "The ordinary case.",
        "example": "<Breadcrumbs\n  items={[\n    { label: \"Home\", href: \"/\" },\n    { label: \"Reports\", href: \"/reports\" },\n    { label: \"Q3 revenue\" },\n  ]}\n/>",
        "notes": "The last item has no `href` — it is the page you are on. It renders as text with `aria-current=\"page\"` rather than a link to itself."
      },
      {
        "case": "One landmark among several",
        "when": "A page that also has a primary nav and a pagination nav.",
        "example": "<Breadcrumbs label=\"Breadcrumb\" items={trail} />",
        "notes": "Distinct `aria-label`s are what make landmarks tellable apart in a screen reader's landmark list. Two navs both called 'Navigation' are worse than one."
      }
    ],
    "behavior": null,
    "tokens": {
      "base": {
        "summary": "A trail of links ending in the current page.",
        "tokens": {
          "gap between crumbs": "space.gap.xs",
          "font-size": "type.supporting.size — a trail is orientation, not content, so it sits below body size",
          "link color": "theme.accent-role.fg — the token documented for an inline link",
          "current page color": "theme.fg.primary — the end of the trail is where you are, so it reads as text rather than as another link",
          "current page font-weight": "type.control.weight",
          "separator color": "theme.fg.muted — punctuation, quieter than either crumb",
          "outline (focus-visible)": "focus.ring-width solid theme.focus-ring, offset focus.ring-offset",
          "transition": "motion.interactive.duration with motion.interactive.easing"
        }
      }
    },
    "example": "<Breadcrumbs\n        label=\"Breadcrumb\"\n        items={[\n          { label: \"Settings\", href: \"#\" },\n          { label: \"Billing\" },\n        ]}\n      />"
  },
  {
    "name": "button",
    "title": "Button",
    "description": "Primary, secondary, tertiary, and destructive actions in three sizes, with loading and icon-only modes.",
    "family": "buttons",
    "tier": "free",
    "status": {
      "css": {
        "state": "latest",
        "version": "0.1.0"
      },
      "react": {
        "state": "latest",
        "version": "0.1.0"
      },
      "figma": {
        "state": "future"
      }
    },
    "dependencies": [
      "state-layer",
      "spinner"
    ],
    "importPath": "@rata/react",
    "mode": "documented",
    "note": null,
    "extends": "Omit<ButtonHTMLAttributes<HTMLButtonElement>, \"disabled\">",
    "props": [
      {
        "name": "variant",
        "optional": true,
        "type": "ButtonVariant",
        "description": null,
        "default": "\"primary\"",
        "values": [
          "primary",
          "secondary",
          "tertiary",
          "destructive"
        ],
        "declared": true,
        "summary": "Which action tier this button occupies, and therefore how loud it is.",
        "use": [
          "`primary` for the single highest-priority action in a view — one per view.",
          "`secondary` for a supporting action shown beside a primary one.",
          "`tertiary` for low-emphasis actions in dense areas, like a row action.",
          "`destructive` for an action that deletes or revokes something."
        ],
        "dont": [
          "Two `primary` buttons in one view — the tier is a claim about priority, and two claims cancel out.",
          "`destructive` for merely irreversible actions like Submit; it means data is lost."
        ],
        "conflicts": [],
        "a11y": "Variant is visual only. A destructive action still needs its own confirmation step — colour is not a warning."
      },
      {
        "name": "size",
        "optional": true,
        "type": "ButtonSize",
        "description": null,
        "default": "\"md\"",
        "values": [
          "sm",
          "md",
          "lg"
        ],
        "declared": true,
        "summary": "Control height and inline padding, from the `size.control.*` scale. Text size does not change with it.",
        "use": [
          "`md` everywhere by default.",
          "`sm` inside dense rows, toolbars, and table cells.",
          "`lg` for a single prominent call to action on a marketing or empty-state surface."
        ],
        "dont": [
          "Shrinking to `sm` to fit a cramped layout — fix the layout budget instead.",
          "Expecting the label to grow with it. All three sizes set the same text size, so `size` buys a bigger target for the same action rather than a louder one — and an icon inside, being sized from that text, does not change either.",
          "Setting height or padding yourself; the size prop is the only supported way to change either."
        ],
        "conflicts": [],
        "a11y": null
      },
      {
        "name": "disabled",
        "optional": true,
        "type": "boolean",
        "description": null,
        "declared": true,
        "summary": "Blocks activation while keeping the button focusable and announced.",
        "use": [
          "An action that is unavailable for a reason the surrounding UI already explains."
        ],
        "dont": [
          "Hiding why an action is unavailable — pair it with visible helper text or a tooltip.",
          "Form validation gating, where an inline error explains more than a dead button does."
        ],
        "conflicts": [
          "loading"
        ],
        "a11y": "Emits `aria-disabled`, never the native `disabled` attribute: the control stays in the tab order and discoverable by screen readers, and activation is blocked in the primitive's click guard."
      },
      {
        "name": "loading",
        "optional": true,
        "type": "boolean",
        "description": "Shows a spinner and blocks activation while preserving focus.",
        "declared": true,
        "summary": "Shows a spinner and refuses activation while an operation is in flight.",
        "use": [
          "Async work started by this button, so the same control reports its own progress."
        ],
        "dont": [
          "Page-level or unrelated loading — that belongs to the region that is actually loading.",
          "Swapping the label to 'Loading…' as well; the spinner and `aria-busy` already say it, and a changing label loses the action's name."
        ],
        "conflicts": [
          "disabled"
        ],
        "a11y": "Emits `aria-busy` alongside `aria-disabled`, so assistive technology hears both that the control is working and that it will not respond yet."
      },
      {
        "name": "iconOnly",
        "optional": true,
        "type": "boolean",
        "description": "Square icon-only button; pass the icon as children and set aria-label.",
        "declared": true,
        "summary": "Square button carrying an icon and no visible text.",
        "use": [
          "Dense toolbars and row actions where the icon is unambiguous and repeated.",
          "Icon-only buttons in this system: there is no separate `IconButton` component, and one would be a second way to do what this prop already does."
        ],
        "dont": [
          "Any action whose icon is not already conventional — an unlabelled novel icon is unreadable.",
          "Passing text children as well; the square sizing assumes a single glyph."
        ],
        "conflicts": [],
        "a11y": "You MUST pass `aria-label`. Nothing in the component supplies an accessible name when there is no visible text."
      }
    ],
    "usage": [
      {
        "case": "Primary action with async work",
        "when": "The most common pairing: one primary button that owns its own progress.",
        "example": "<Button loading={saving} onClick={save}>Save changes</Button>",
        "notes": "`loading` blocks repeat activation on its own — don't also guard with `disabled`."
      },
      {
        "case": "Destructive action in a confirmation",
        "when": "Deleting something, with the safe action given equal visual weight beside it.",
        "example": "<Button variant=\"destructive\" onClick={remove}>Delete project</Button>"
      },
      {
        "case": "Icon-only row action",
        "when": "A dense table row where a labelled button would not fit.",
        "example": "<Button variant=\"tertiary\" size=\"sm\" iconOnly aria-label=\"Remove row\">×</Button>",
        "notes": "Without `aria-label` this button has no accessible name at all."
      }
    ],
    "behavior": {
      "primitive": "getButtonProps",
      "summary": "Activation guarding and disabled/busy semantics, independent of React and of any styling.",
      "decisions": [
        {
          "decision": "`aria-disabled` instead of the native `disabled` attribute",
          "why": "A natively disabled button leaves the tab order and stops being announced, so users navigating by keyboard or screen reader cannot discover that the action exists. Activation is blocked in the click guard instead."
        },
        {
          "decision": "`loading` implies inert",
          "why": "One flag covers both halves of what callers actually want — show progress, and refuse a second click — so the two cannot be set inconsistently."
        }
      ]
    },
    "tokens": {
      "primary": {
        "summary": "The single highest-priority action in a view.",
        "tokens": {
          "background": "theme.accent-role.bg",
          "color": "theme.fg.on-accent",
          "height": "size.control.md — and size.control.sm / .lg at the other sizes; this is the only thing `size` changes",
          "padding-inline": "space.control.padding-inline.md",
          "gap": "space.gap.xs",
          "border-radius": "radius.element",
          "font-size": "type.control.size.md — constant across all three sizes, so `size` changes the box and not the label",
          "font-weight": "type.control.weight",
          "line-height": "type.control.line-height.md — constant, as with font-size",
          "transition-duration": "motion.interactive.duration",
          "transition-timing-function": "motion.interactive.easing",
          "outline (focus-visible)": "focus.ring-width solid theme.focus-ring, offset focus.ring-offset",
          "hover/press": "compose the .rata-state-layer class — do not swap the background"
        }
      },
      "secondary": {
        "summary": "A supporting action shown next to a primary one. Its label is accent-toned rather than neutral, so the outlined button still reads as an action and not as a disabled box — the border alone carries very little at this fill.",
        "tokens": {
          "background": "theme.secondary-role.bg",
          "color": "theme.accent-role.fg",
          "border-color": "theme.secondary-role.border",
          "height": "size.control.md — and size.control.sm / .lg at the other sizes; this is the only thing `size` changes",
          "border-radius": "radius.element",
          "hover/press": "compose the .rata-state-layer class"
        }
      },
      "destructive": {
        "summary": "An action that deletes or revokes something.",
        "tokens": {
          "background": "theme.danger-role.bg",
          "color": "theme.fg.on-accent",
          "height": "size.control.md — and size.control.sm / .lg at the other sizes; this is the only thing `size` changes",
          "border-radius": "radius.element",
          "hover/press": "compose the .rata-state-layer class"
        }
      }
    },
    "example": "<Button>Publish</Button>"
  },
  {
    "name": "button-group",
    "title": "Button Group",
    "description": "A named set of related buttons, spaced or attached into one bar, horizontal or vertical.",
    "family": "buttons",
    "tier": "free",
    "status": {
      "css": {
        "state": "latest",
        "version": "0.1.0"
      },
      "react": {
        "state": "latest",
        "version": "0.1.0"
      },
      "figma": {
        "state": "future"
      }
    },
    "dependencies": [
      "button"
    ],
    "importPath": "@rata/react",
    "mode": "documented",
    "note": null,
    "extends": "Omit<HTMLAttributes<HTMLDivElement>, \"role\">",
    "props": [
      {
        "name": "label",
        "optional": true,
        "type": "string",
        "description": "Accessible name for the group. Required unless `labelledBy` names an existing element.",
        "declared": true,
        "summary": "Accessible name for the group — what these buttons have in common.",
        "use": [
          "Always, unless `labelledBy` points at a visible heading that already says it.",
          "Naming the group by its subject rather than its shape: \"Row actions\", not \"Button group\"."
        ],
        "dont": [
          "Restating the buttons' own labels — \"Save and Cancel\" tells a screen reader what it is about to read anyway.",
          "Leaving it off because the group looks obvious on screen; an unnamed group adds a level to navigate and says nothing about what is inside it."
        ],
        "conflicts": [
          "labelledBy"
        ],
        "a11y": "Becomes the group's `aria-label`. Every group MUST have a name from this or from `labelledBy` — the component cannot supply one, because only the caller knows what the buttons have in common."
      },
      {
        "name": "labelledBy",
        "optional": true,
        "type": "string",
        "description": "Id of an element that already names this group — a heading above it, typically.",
        "declared": true,
        "summary": "Id of an element that already names this group, used instead of a written label.",
        "use": [
          "A visible heading sits directly above the group and says the same thing a label would."
        ],
        "dont": [
          "Pointing at an element that is conditionally rendered — a dangling `aria-labelledby` leaves the group unnamed with no visible sign of it."
        ],
        "conflicts": [
          "label"
        ],
        "a11y": "Wins over `label` when both are passed, because it points at text the user can see: when the visible heading changes, the name changes with it."
      },
      {
        "name": "orientation",
        "optional": true,
        "type": "ButtonGroupOrientation",
        "description": null,
        "default": "\"horizontal\"",
        "values": [
          "horizontal",
          "vertical"
        ],
        "declared": true,
        "summary": "Which way the buttons stack.",
        "use": [
          "`vertical` in a narrow rail or a menu-like column where a row would wrap."
        ],
        "dont": [
          "`vertical` as a substitute for a menu — a list of navigation choices is a menu, not a stack of buttons.",
          "Switching orientation per breakpoint by swapping the prop mid-interaction; pick one per layout and let the group wrap if it must."
        ],
        "conflicts": [],
        "a11y": "Presentational only. It sets a data attribute for styling and deliberately does not set `aria-orientation`, which would promise arrow-key navigation this group does not implement."
      },
      {
        "name": "attached",
        "optional": true,
        "type": "boolean",
        "description": "Joins the buttons into one continuous bar, with only the outer corners rounded.",
        "declared": true,
        "summary": "Joins the buttons into one continuous bar, with shared borders and only the outer corners rounded.",
        "use": [
          "Actions that are variations of one thing and are read as a unit — a pager's Previous/Next, a split of one action into its two directions."
        ],
        "dont": [
          "Actions of different weight. Attaching a primary to a destructive makes them look like one control with two halves, which is exactly the moment a mis-click costs the most.",
          "Expressing a choice. Attached buttons that hold a selected state are a `toggle-button-group`, and it says so to assistive technology; this one does not."
        ],
        "conflicts": [],
        "a11y": "Visual only — nothing about the attached form changes what is announced. The buttons stay separately focusable either way."
      }
    ],
    "usage": [
      {
        "case": "Two actions at the end of a form",
        "when": "The default case: a primary action and its escape hatch, spaced rather than joined.",
        "example": "<ButtonGroup label=\"Form actions\">\n  <Button variant=\"secondary\">Cancel</Button>\n  <Button>Save changes</Button>\n</ButtonGroup>",
        "notes": "Spaced, not attached — these are two different decisions, and joining them makes them read as one control."
      },
      {
        "case": "Attached pager",
        "when": "Two halves of the same action, which is what `attached` is for.",
        "example": "<ButtonGroup label=\"Pagination\" attached>\n  <Button variant=\"secondary\" iconOnly aria-label=\"Previous page\">‹</Button>\n  <Button variant=\"secondary\" iconOnly aria-label=\"Next page\">›</Button>\n</ButtonGroup>",
        "notes": "`iconOnly` still requires its own `aria-label` on each button — the group's name says what the pair is, not what each half does."
      },
      {
        "case": "Named by a heading that is already on screen",
        "when": "The group sits under a heading that says exactly what the label would have said.",
        "example": "<h3 id=\"export-actions\">Export</h3>\n<ButtonGroup labelledBy=\"export-actions\">\n  <Button variant=\"secondary\">CSV</Button>\n  <Button variant=\"secondary\">JSON</Button>\n</ButtonGroup>",
        "notes": "Prefer this to a written `label` whenever the heading exists: one string, visible, and it cannot drift from what the group actually contains."
      },
      {
        "case": "Vertical rail",
        "when": "A narrow column where a horizontal row would wrap into an unreadable grid.",
        "example": "<ButtonGroup label=\"Layer actions\" orientation=\"vertical\">\n  <Button variant=\"tertiary\">Bring forward</Button>\n  <Button variant=\"tertiary\">Send backward</Button>\n</ButtonGroup>",
        "notes": "Still tab-navigated, not arrow-navigated. If the column is long enough that tabbing through it is tedious, it wants to be a menu."
      }
    ],
    "behavior": {
      "primitive": "getButtonGroupProps",
      "summary": "The group's accessible name and its grouping role — the two things the buttons inside cannot own between them. No selection state and no value: a button group runs actions, it does not hold an answer.",
      "decisions": [
        {
          "decision": "`role=\"group\"` with a required accessible name, not `role=\"toolbar\"`",
          "why": "A toolbar is a single tab stop with arrow-key navigation between its controls. That is right for a dense, persistent bar of many actions and wrong for the common case here — two or three buttons in a card footer — where collapsing them into one tab stop makes them harder to reach, not easier. Groups that genuinely want toolbar semantics should say so explicitly rather than getting them by accident from a layout wrapper."
        },
        {
          "decision": "Each button keeps its own tab stop",
          "why": "Follows from the role decision above. It also keeps the group composable: a `Button` inside a group behaves exactly like a `Button` outside one, so nothing about focus or activation has to be re-learned."
        },
        {
          "decision": "Orientation is a data attribute, not `aria-orientation`",
          "why": "Orientation here changes how the group stacks and nothing else. `aria-orientation` announces a navigation direction, which would promise arrow-key movement this group deliberately does not implement."
        },
        {
          "decision": "No per-item position data — corner joining is `:first-child`/`:last-child` in CSS",
          "why": "The DOM already knows which button is first and last. Emitting `data-position` would be a second copy of that fact, and a copy that goes stale the moment a button is conditionally rendered."
        },
        {
          "decision": "Not a selection control",
          "why": "A group whose items hold a pressed state is `toggle-button-group`: it needs a value, a selection mode, and roving focus, none of which belong in a container for plain actions. Keeping them separate is what stops this component growing a `value` prop that only makes sense half the time."
        }
      ]
    },
    "tokens": {
      "spaced": {
        "summary": "The default: buttons that belong together but read as separate controls.",
        "tokens": {
          "gap": "space.gap.sm",
          "item border-radius": "radius.element — each button keeps the corners it sets for itself"
        }
      },
      "attached": {
        "summary": "Buttons joined into one continuous bar.",
        "tokens": {
          "gap": "space.0",
          "inner corner border-radius": "radius.none",
          "outer corner border-radius": "radius.element, as logical corners so the bar flips in RTL",
          "collapsed shared border": "negative margin of border.1, so adjacent borders read as one",
          "focused item stacking": "raised above its neighbours so the focus ring is not clipped — a stacking fix, not a token"
        }
      }
    },
    "example": "<ButtonGroup label=\"Form actions\">\n        <Button variant=\"secondary\">Cancel</Button>\n        <Button>Save changes</Button>\n      </ButtonGroup>"
  },
  {
    "name": "checkbox",
    "title": "Checkbox",
    "description": "A single on/off choice, with an indeterminate state for a parent summarising its children.",
    "family": "inputs",
    "tier": "free",
    "status": {
      "css": {
        "state": "latest",
        "version": "0.1.0"
      },
      "react": {
        "state": "latest",
        "version": "0.1.0"
      },
      "figma": {
        "state": "future"
      }
    },
    "dependencies": [
      "state-layer"
    ],
    "importPath": "@rata/react",
    "mode": "documented",
    "note": null,
    "extends": "Omit<",
    "props": [
      {
        "name": "label",
        "optional": false,
        "type": "ReactNode",
        "description": "The checkbox's label. Always required — `labelHidden` hides it, nothing removes it.",
        "declared": true,
        "summary": "The checkbox's label. Always required — `labelHidden` can take it off screen, but nothing removes it.",
        "use": [
          "Always. A checkbox with no label is a control nobody can describe."
        ],
        "dont": [
          "Using the surrounding sentence as the label and leaving this empty — the binding is what makes the text a hit target and an accessible name.",
          "Passing an empty string. An empty label is an unnamed control; hide it with `labelHidden` instead."
        ],
        "conflicts": [],
        "a11y": "Rendered as a real `<label for>` bound to the input, so clicking the text toggles the box. Still a real label when hidden — only its pixels go away."
      },
      {
        "name": "labelHidden",
        "optional": true,
        "type": "boolean",
        "description": "Takes the label off screen while leaving it in the accessibility tree.",
        "declared": true,
        "summary": "Takes the label off screen while leaving it in the accessibility tree.",
        "use": [
          "A checkbox in a table row or header cell, where the column already names what is being toggled and repeating it per row would be noise.",
          "A dense control panel where the box sits directly under a heading that names it."
        ],
        "dont": [
          "Hiding it to save space in a form. In a form the label is the last thing to cut; if space is short the layout budget is wrong.",
          "Hiding it on a standalone checkbox. With no adjacent text naming it, the label is the only thing saying what agreeing to this means.",
          "Hiding it and then relying on a neighbouring cell's position to explain the box — position is not a name, and it is the first thing lost when a layout reflows."
        ],
        "conflicts": [],
        "a11y": "The label element is still rendered and still bound by `for`, so the accessible name is unchanged — it is clipped, not removed. Not `display: none`, which would take the name with it. The required marker goes off screen with it; `aria-required` still carries that."
      },
      {
        "name": "checked",
        "optional": true,
        "type": "boolean",
        "description": "Whether the box is checked. Makes the component controlled.",
        "declared": true,
        "summary": "Whether the box is checked. Makes the component controlled.",
        "use": [
          "Whenever the value lives outside the component, which is most of the time."
        ],
        "dont": [
          "Passing it without `onCheckedChange`; the box would render its state and then refuse to change it."
        ],
        "conflicts": [
          "defaultChecked"
        ],
        "a11y": "Drives the native checked state, which is what assistive technology reports."
      },
      {
        "name": "defaultChecked",
        "optional": true,
        "type": "boolean",
        "description": "Starting state for an uncontrolled checkbox. Conflicts with `checked`.",
        "declared": true,
        "summary": "Starting state for an uncontrolled checkbox.",
        "use": [
          "Genuinely local state nothing else reads."
        ],
        "dont": [],
        "conflicts": [
          "checked"
        ],
        "a11y": null
      },
      {
        "name": "onCheckedChange",
        "optional": true,
        "type": "(checked: boolean, event: { preventDefault(): void }) => void",
        "description": "Called with the state the box should move to, not the one it is leaving.",
        "declared": true,
        "summary": "Called with the state the box should move to, not the one it is leaving.",
        "use": [],
        "dont": [
          "Reading `event.target.checked` instead of the argument — they agree, but the argument is the one this component promises."
        ],
        "conflicts": [],
        "a11y": null
      },
      {
        "name": "indeterminate",
        "optional": true,
        "type": "boolean",
        "description": "A parent summarising children that disagree. Outranks `checked`.",
        "default": "false",
        "declared": true,
        "summary": "Shows the box as neither checked nor unchecked: a parent summarising children that disagree.",
        "use": [
          "A 'select all' box above a list where only some rows are selected."
        ],
        "dont": [
          "Expressing 'unknown' or 'not yet answered'. It means partially checked, and a screen reader announces it as `mixed`.",
          "Combining it with a `checked` that is also true and expecting both to show — indeterminate wins visually and in what is announced, which is why they are not one prop."
        ],
        "conflicts": [],
        "a11y": "Announced as `aria-checked=\"mixed\"`. Set as a DOM property by this component, because HTML has no such attribute."
      },
      {
        "name": "description",
        "optional": true,
        "type": "ReactNode",
        "description": "Helper text under the label.",
        "declared": true,
        "summary": "Helper text under the label.",
        "use": [],
        "dont": [
          "Error text. A checkbox that is invalid belongs in a field wrapper that owns the message."
        ],
        "conflicts": [],
        "a11y": null
      },
      {
        "name": "id",
        "optional": true,
        "type": "string",
        "description": "Stable id for the input. Defaults to a generated one.",
        "declared": true,
        "summary": "Stable id for the input. Defaults to a generated one.",
        "use": [
          "Linking an external label, or a scroll-to-error that needs the id to survive a re-render."
        ],
        "dont": [
          "Reusing one id across two checkboxes — the derived description id would collide too."
        ],
        "conflicts": [],
        "a11y": null
      },
      {
        "name": "disabled",
        "optional": true,
        "type": "boolean",
        "description": "Blocks activation while keeping the box focusable and announced.",
        "declared": true,
        "summary": "Blocks activation while keeping the box focusable and announced.",
        "use": [],
        "dont": [],
        "conflicts": [],
        "a11y": "Emits `aria-disabled` and refuses activation. Never the native `disabled` attribute, which would remove it from the tab order."
      },
      {
        "name": "required",
        "optional": true,
        "type": "boolean",
        "description": "Marks the checkbox as required.",
        "declared": true,
        "summary": "Marks the checkbox as required to assistive technology and in the label.",
        "use": [],
        "dont": [],
        "conflicts": [],
        "a11y": "Emits `aria-required`. The visible marker is the label's job."
      },
      {
        "name": "className",
        "optional": true,
        "type": "string",
        "description": null,
        "declared": true,
        "summary": "Class for the checkbox's wrapper.",
        "use": [
          "Placing the component in its surrounding layout — a span, a width, a margin the parent cannot express."
        ],
        "dont": [
          "Restyling the box. Its size, border and fill are the approved mapping.",
          "Restyling the component's internals through descendant selectors. Those are the approved token mapping, and overriding them here puts a second answer somewhere the contract cannot see."
        ],
        "conflicts": [],
        "a11y": null
      }
    ],
    "usage": [
      {
        "case": "A single consent box",
        "when": "The commonest case: one box, one label, its own state.",
        "example": "<Checkbox\n  label=\"Email me product updates\"\n  checked={subscribed}\n  onCheckedChange={setSubscribed}\n/>"
      },
      {
        "case": "A parent summarising a list",
        "when": "Some rows selected, so the parent is neither checked nor unchecked.",
        "example": "<Checkbox\n  label=\"Select all\"\n  checked={all}\n  indeterminate={some && !all}\n  onCheckedChange={toggleAll}\n/>",
        "notes": "`indeterminate` is what a screen reader hears as `mixed`. It is deliberately separate from `checked`, because it answers a different question: not this box's value, but whether the children below agree."
      },
      {
        "case": "Unavailable but still readable",
        "when": "A choice that cannot be made yet, whose current state still matters.",
        "example": "<Checkbox label=\"Ship to billing address\" checked disabled />",
        "notes": "aria-disabled and a refused activation, never the native `disabled` attribute — the box stays focusable so a keyboard user can find it and read its state."
      },
      {
        "case": "A column of boxes under a heading that names them",
        "when": "A table or a settings grid where the column header already says what is being toggled.",
        "example": "<Checkbox label=\"Notify by email\" labelHidden checked={row.email} onCheckedChange={…} />",
        "notes": "`labelHidden` hides the pixels, not the name: the `<label for>` is still rendered and still bound, so each box is announced as \"Notify by email\" exactly as it would be with the label visible. The hit target shrinks to the box itself, which is the cost of this choice — it is why the visible label is the default."
      }
    ],
    "behavior": {
      "primitive": "getCheckboxProps",
      "summary": "Checked state, the indeterminate third appearance, and the activation guard. Built on a native input[type=checkbox] so keyboard, form participation and the checked semantics come from the platform rather than being re-implemented.",
      "decisions": [
        {
          "decision": "A native input, not a div with role=\"checkbox\"",
          "why": "Space to toggle, participation in a form's data, and the checked state itself are all free from the platform and all easy to get subtly wrong by hand. The visual is drawn by a sibling element; the input stays in the DOM and keeps the semantics."
        },
        {
          "decision": "`indeterminate` is a separate prop, not a third value of `checked`",
          "why": "It answers a different question. `checked` is this control's own value; `indeterminate` says the control is standing in for a set of children that disagree. A tri-state `checked` would force every caller to handle a value that only a parent checkbox can ever have, and would make the common two-state case read as incomplete."
        },
        {
          "decision": "Indeterminate is set as a DOM property, never an attribute",
          "why": "There is no `indeterminate` content attribute in HTML — only an IDL property — so it cannot be expressed in JSX and has to be assigned to the element. The wrapper owns that, which is the whole reason this component cannot be a bare styled input."
        },
        {
          "decision": "Activation is guarded, not disabled",
          "why": "This system's disabled controls stay focusable and announced. `readOnly` is the input analogue used by text-field, but it has no effect on a checkbox, so the guard here is a refused activation plus aria-disabled — the same shape as the button primitive."
        },
        {
          "decision": "The label is required and is a real `<label for>`",
          "why": "A checkbox's hit target should include its text, and a bound label gives that for free. It also means the accessible name cannot drift from what is on screen."
        }
      ]
    },
    "tokens": {
      "base": {
        "summary": "A single on/off choice, with a third appearance for a parent summarising its children.",
        "tokens": {
          "box size": "size.icon.md — the box is sized to the glyph it holds, so it takes the icon scale rather than the control one; the control heights are row heights and would dwarf the box itself",
          "row height": "no reserved row height. An earlier version set it to size.control.md for a bigger hit target, but with the box aligned to the first line that reserved 12px of dead space under every row and inflated the list's pitch to 40px. At the resulting 28px pitch the target conforms through WCAG 2.5.8's spacing exception: 24px circles centred on adjacent targets do not intersect. The label is bound, so the target is the full width of the text as well",
          "control height": "one line tall, from calc(type.control.size.md x type.control.line-height.md) = 20px. The line-height token is a unitless ratio, so it is not a length on its own — used alone the browser drops the declaration",
          "border": "border.default solid theme.border.strong — the same boundary tone text-field uses, tone-walked to the 3:1 WCAG 1.4.11 asks of a control's edge",
          "gap between box and label": "space.gap.sm — the inline role, because this pairing is horizontal",
          "gap between label and description": "space.stack.2xs",
          "label color": "theme.fg.primary",
          "label font-size": "type.control.size.md",
          "description color": "theme.fg.secondary",
          "description font-size": "type.supporting.size",
          "checked background": "theme.accent-role.bg",
          "hover/press": "compose the .rata-state-layer class — never a hand-written hover rule",
          "outline (focus-visible)": "focus.ring-width solid theme.focus-ring, offset focus.ring-offset — the offset is kept here, unlike text-field: a 20px box has no room for a flush ring to read as separate from its own border",
          "opacity (aria-disabled)": "state.disabled-opacity",
          "transition": "motion.interactive.duration with motion.interactive.easing",
          "border-radius": "radius.inner — a nested square, which is what radius.inner names",
          "checked mark": "theme.fg.on-accent — the only foreground permitted on that fill, and it inverts with the accent in the dark scheme",
          "indeterminate mark": "theme.fg.on-accent on theme.accent-role.bg, drawn as a bar rather than a tick. The fill matches checked because the state is 'partly checked', not a third colour"
        }
      }
    },
    "example": "<Checkbox label=\"Ship to billing address\" checked disabled />"
  },
  {
    "name": "dialog",
    "title": "Dialog",
    "description": "Modal dialog with focus trapping, scroll locking, and dismiss behavior.",
    "family": "overlays",
    "tier": "free",
    "status": {
      "css": {
        "state": "future"
      },
      "react": {
        "state": "future"
      },
      "figma": {
        "state": "future"
      }
    },
    "dependencies": [],
    "importPath": "@rata/react",
    "mode": "spec",
    "note": "No React implementation yet (status: future). There is nothing to look up — don't invent props for this one.",
    "extends": null,
    "props": [],
    "usage": [],
    "behavior": null,
    "tokens": {
      "base": {
        "summary": "A modal surface that takes over the screen.",
        "tokens": {
          "background": "theme.bg.surface",
          "color": "theme.fg.primary",
          "border-radius": "radius.page",
          "padding": "space.padding.lg",
          "box-shadow": "theme.elevation.modal",
          "title font-size": "type.heading.size",
          "title font-weight": "type.heading.weight",
          "gap between title, body, and footer": "space.stack.md",
          "enter transition": "motion.modal.duration with motion.easing.enter",
          "exit transition": "motion.modal.duration with motion.easing.exit"
        }
      }
    },
    "example": null
  },
  {
    "name": "icon",
    "title": "Icon",
    "description": "A Lucide glyph sized from the size.icon.* scale, inheriting its colour from the text around it.",
    "family": "content",
    "tier": "free",
    "status": {
      "css": {
        "state": "latest",
        "version": "0.1.0"
      },
      "react": {
        "state": "latest",
        "version": "0.1.0"
      },
      "figma": {
        "state": "future"
      }
    },
    "dependencies": [],
    "importPath": "@rata/icons",
    "mode": "documented",
    "note": null,
    "extends": null,
    "props": [
      {
        "name": "icon",
        "optional": false,
        "type": "LucideIcon",
        "description": "The Lucide component to render, passed as a value so the set stays tree-shakeable.",
        "declared": true,
        "summary": "The Lucide component to render, passed as a value rather than a name.",
        "use": [
          "Any icon imported from `@rata/icons` — the curated set — or from `lucide-react` directly when the set is missing one.",
          "Passing the component itself (`icon={Trash2}`), which is what keeps the icon set tree-shakeable: a string name would force every icon into the bundle."
        ],
        "dont": [
          "Rendering the Lucide component directly instead of through `Icon`. It would take Lucide's default 24px rather than a step from the scale, and it would not get the a11y switch below.",
          "An emoji or a text glyph as a stand-in. Those inherit font metrics rather than the icon scale, so they will not line up with real icons beside them."
        ],
        "conflicts": [],
        "a11y": "Rendered `aria-hidden` unless `label` is set — see `label`."
      },
      {
        "name": "size",
        "optional": true,
        "type": "IconSize",
        "description": "Which step of the `size.icon.*` scale to draw at. Defaults to following the surrounding text.",
        "default": "\"text\"",
        "values": [
          "text",
          "xs",
          "sm",
          "md",
          "lg"
        ],
        "declared": true,
        "summary": "Which step of the `size.icon.*` scale the glyph is drawn at. Defaults to following the surrounding text.",
        "use": [
          "Leave it alone for anything with text near it — the default `text` step is 1.15em, so the glyph tracks whatever font-size it lands in and needs no decision per control.",
          "`xs` / `sm` / `md` / `lg` only for a standalone glyph with no text to inherit from: a status dot in a table cell, or an icon-only control whose name is an `aria-label` rather than visible text.",
          "`lg` when the glyph should render unscaled — 24px is Lucide's native artboard."
        ],
        "dont": [
          "Picking a fixed step to match a control's size. A control's `size` changes its box, not its text, so an icon inside it should not change either — and a fixed step stops matching the moment the surrounding text does change.",
          "Mixing steps within one row of icons — they read as a set, so they measure as a set."
        ],
        "conflicts": [],
        "a11y": null
      },
      {
        "name": "label",
        "optional": true,
        "type": "string",
        "description": "Accessible name, for an icon that carries meaning nothing else conveys.",
        "declared": true,
        "summary": "Accessible name, for an icon that carries meaning nothing else conveys.",
        "use": [
          "An icon that is the only thing communicating something — a status glyph in a table cell with no adjacent text."
        ],
        "dont": [
          "An icon inside a `Button` with `iconOnly`. That button already carries the `aria-label` the contract requires, so labelling the icon too announces the same thing twice.",
          "An icon sitting beside a visible text label. The text is the name; the icon is decoration and must stay hidden from assistive technology."
        ],
        "conflicts": [],
        "a11y": "Present, the glyph becomes `role=\"img\"` with this as its name. Absent, it is `aria-hidden` — the same switch `Spinner` uses for the same reason, and the reason the decorative case needs no thought to get right."
      },
      {
        "name": "className",
        "optional": true,
        "type": "string",
        "description": null,
        "declared": true,
        "summary": "Extra classes on the glyph, for placement — not for restyling it.",
        "use": [
          "A layout concern the parent cannot express, such as a margin token when the icon is not already inside a flex row with a gap."
        ],
        "dont": [
          "Overriding the size. The four steps are the scale; a fifth size written in a component's CSS is the drift `size.icon.*` exists to prevent — add a step to the scale instead.",
          "Setting a colour. The glyph strokes with `currentColor` so it always matches the text it sits in; colouring it separately is how an icon ends up contradicting its own label."
        ],
        "conflicts": [],
        "a11y": null
      }
    ],
    "usage": [
      {
        "case": "Inside an icon-only button",
        "when": "The most common pairing, and the one where the a11y split matters most.",
        "example": "<Button iconOnly aria-label=\"Delete row\">\n  <Icon icon={Trash2} />\n</Button>",
        "notes": "No `label` on the icon: the button owns the accessible name, so the glyph stays `aria-hidden`. `size` is left at `md`, which is what a 32px control wants."
      },
      {
        "case": "Beside a text label",
        "when": "An action whose icon reinforces a word rather than replacing it.",
        "example": "<Button>\n  <Icon icon={Download} />\n  Export CSV\n</Button>",
        "notes": "No `size`: the default `text` step is 1.15em, so the glyph is sized from the button's own label and stays matched to it. The icon is decorative here, so it takes no `label` either."
      },
      {
        "case": "Carrying meaning on its own",
        "when": "A status glyph in a dense row, with no adjacent text to name it.",
        "example": "<Icon icon={CircleAlert} size=\"xs\" label=\"Needs review\" />",
        "notes": "This is the only case that takes a `label`. Without one the row would announce nothing at all where a sighted user sees a warning."
      }
    ],
    "behavior": null,
    "tokens": {
      "base": {
        "summary": "A glyph sized from the text around it, taking its colour from that text too.",
        "tokens": {
          "width": "size.icon.text — 1.15em, so it follows the inherited font-size. size.icon.xs / .sm / .md / .lg for a standalone glyph.",
          "height": "same step as width; the scale is one number and the glyph is square",
          "stroke": "currentColor — the icon inherits the colour of the text it sits in, so it needs no colour token of its own and cannot contradict its label",
          "flex-shrink": "0 — a glyph beside text must not be squeezed by it, which is a layout fact rather than a token"
        }
      }
    },
    "example": "<Icon icon={Bold} />"
  },
  {
    "name": "menu",
    "title": "Menu",
    "description": "A transient list of actions anchored to a trigger.",
    "family": "overlays",
    "tier": "free",
    "status": {
      "css": {
        "state": "future"
      },
      "react": {
        "state": "future"
      },
      "figma": {
        "state": "future"
      }
    },
    "dependencies": [
      "state-layer"
    ],
    "importPath": "@rata/react",
    "mode": "spec",
    "note": "No React implementation yet (status: future). There is nothing to look up — don't invent props for this one.",
    "extends": null,
    "props": [],
    "usage": [],
    "behavior": null,
    "tokens": {
      "base": {
        "summary": "A transient list of actions anchored to a trigger.",
        "tokens": {
          "background": "theme.bg.surface",
          "border-radius": "radius.container",
          "box-shadow": "theme.elevation.overlay",
          "padding": "space.1",
          "item height": "size.control.sm",
          "item padding-inline": "space.control.padding-inline.sm",
          "item color": "theme.fg.primary",
          "destructive item color": "theme.danger-role.fg",
          "selected item background": "theme.accent-role.subtle",
          "item hover/press": "compose the .rata-state-layer class",
          "separator": "border.default solid theme.border.default",
          "enter transition": "motion.overlay.duration with motion.easing.enter"
        }
      }
    },
    "example": null
  },
  {
    "name": "notice",
    "title": "Notice",
    "description": "An inline banner carrying an informational, success, warning, or error message.",
    "family": "feedback",
    "tier": "free",
    "status": {
      "css": {
        "state": "future"
      },
      "react": {
        "state": "future"
      },
      "figma": {
        "state": "future"
      }
    },
    "dependencies": [],
    "importPath": "@rata/react",
    "mode": "spec",
    "note": "No React implementation yet (status: future). There is nothing to look up — don't invent props for this one.",
    "extends": null,
    "props": [],
    "usage": [],
    "behavior": null,
    "tokens": {
      "base": {
        "summary": "An inline banner. Only the tinted subtle backgrounds carry text; the saturated role fills do not.",
        "tokens": {
          "info background / text": "theme.accent-role.subtle / theme.accent-role.fg",
          "success background / text": "theme.success-role.subtle / theme.success-role.fg",
          "warning background / text": "theme.warning-role.subtle / theme.warning-role.fg",
          "error background / text": "theme.danger-role.subtle / theme.danger-role.fg",
          "padding": "space.padding.sm space.padding.md",
          "border-radius": "radius.element",
          "gap between icon and text": "space.gap.sm",
          "leading accent bar (optional)": "the matching role bg — a non-text use"
        }
      }
    },
    "example": null
  },
  {
    "name": "radio",
    "title": "Radio",
    "description": "One option inside a radio group. Not usable on its own.",
    "family": "inputs",
    "tier": "free",
    "status": {
      "css": {
        "state": "latest",
        "version": "0.1.0"
      },
      "react": {
        "state": "latest",
        "version": "0.1.0"
      },
      "figma": {
        "state": "future"
      }
    },
    "dependencies": [
      "state-layer"
    ],
    "importPath": "@rata/react",
    "mode": "documented",
    "note": null,
    "extends": "Omit<",
    "props": [
      {
        "name": "value",
        "optional": false,
        "type": "string",
        "description": "This option's value. What the group reports when it is chosen.",
        "declared": true,
        "summary": "This option's value. What the group reports when it is chosen.",
        "use": [],
        "dont": [
          "Reusing a value across two radios in one group — the group would consider both chosen."
        ],
        "conflicts": [],
        "a11y": null
      },
      {
        "name": "label",
        "optional": false,
        "type": "ReactNode",
        "description": "The option's label.",
        "declared": true,
        "summary": "The option's label.",
        "use": [],
        "dont": [],
        "conflicts": [],
        "a11y": "A real `<label for>`, so the text is part of the hit target."
      },
      {
        "name": "description",
        "optional": true,
        "type": "ReactNode",
        "description": "Helper text under the label.",
        "declared": true,
        "summary": "Helper text under the label, for an option whose consequence is not obvious.",
        "use": [],
        "dont": [
          "Restating the label in more words."
        ],
        "conflicts": [],
        "a11y": null
      },
      {
        "name": "disabled",
        "optional": true,
        "type": "boolean",
        "description": "Blocks this one option while leaving it focusable and readable.",
        "declared": true,
        "summary": "Blocks this one option while leaving it focusable and readable.",
        "use": [],
        "dont": [],
        "conflicts": [],
        "a11y": "Emits `aria-disabled`. Arrow keys still land on it; it just does not become the answer."
      },
      {
        "name": "className",
        "optional": true,
        "type": "string",
        "description": null,
        "declared": true,
        "summary": "Class for the option's wrapper.",
        "use": [
          "Placing the component in its surrounding layout — a span, a width, a margin the parent cannot express."
        ],
        "dont": [
          "Setting a width to align options — that is the group's layout decision, not the item's.",
          "Restyling the component's internals through descendant selectors. Those are the approved token mapping, and overriding them here puts a second answer somewhere the contract cannot see."
        ],
        "conflicts": [],
        "a11y": null
      }
    ],
    "usage": [
      {
        "case": "Inside its group",
        "when": "The only correct use — and the reason any example of a Radio necessarily shows a group.",
        "example": "<RadioGroup label=\"Support plan\" value={plan} onValueChange={setPlan}>\n  <Radio value=\"standard\" label=\"Standard\" />\n  <Radio value=\"priority\" label=\"Priority\" description=\"One business day response\" />\n  <Radio value=\"dedicated\" label=\"Dedicated\" disabled />\n</RadioGroup>",
        "notes": "Everything that makes this a radio comes from the group: the shared name, the exclusivity, and the arrow-key reachability. That is also why no example can show a Radio on its own — rendered outside a group it throws rather than degrading. Read the specimen as being about the option: its label, its description, and whether it is individually disabled. The question the options answer belongs to RadioGroup."
      }
    ],
    "behavior": {
      "summary": "An option rendered by its group, with no primitive of its own. It holds no state: getRadioGroupProps decides whether this one is checked, whether it is the tab stop, and what happens when it is activated. That is the point — a radio that owned any of those could disagree with its siblings.",
      "decisions": [
        {
          "decision": "A Radio outside a RadioGroup is a mistake, not a fallback",
          "why": "Alone it has no name to share, no siblings to be exclusive with, and no way to be reached by arrow keys — everything that makes it a radio lives in the group. Rendering one standalone would produce a control that looks right and behaves like nothing."
        },
        {
          "decision": "`value` is required and is the item's whole identity",
          "why": "It is what the group compares against, what arrow keys walk, and what a form submits. Without it the group has no way to refer to this option."
        }
      ]
    },
    "tokens": {
      "base": {
        "summary": "One option in a radio group. Circular, so its shape says 'one of these' before the label is read.",
        "tokens": {
          "box size": "size.icon.md — the box is sized to the glyph it holds, so it takes the icon scale rather than the control one; the control heights are row heights and would dwarf the box itself",
          "row height": "no reserved row height. An earlier version set it to size.control.md for a bigger hit target, but with the box aligned to the first line that reserved 12px of dead space under every row and inflated the list's pitch to 40px. At the resulting 28px pitch the target conforms through WCAG 2.5.8's spacing exception: 24px circles centred on adjacent targets do not intersect. The label is bound, so the target is the full width of the text as well",
          "control height": "one line tall, from calc(type.control.size.md x type.control.line-height.md) = 20px. The line-height token is a unitless ratio, so it is not a length on its own — used alone the browser drops the declaration",
          "border": "border.default solid theme.border.strong — the same boundary tone text-field uses, tone-walked to the 3:1 WCAG 1.4.11 asks of a control's edge",
          "gap between box and label": "space.gap.sm — the inline role, because this pairing is horizontal",
          "gap between label and description": "space.stack.2xs",
          "label color": "theme.fg.primary",
          "label font-size": "type.control.size.md",
          "description color": "theme.fg.secondary",
          "description font-size": "type.supporting.size",
          "checked background": "theme.accent-role.bg",
          "hover/press": "compose the .rata-state-layer class — never a hand-written hover rule",
          "outline (focus-visible)": "focus.ring-width solid theme.focus-ring, offset focus.ring-offset — the offset is kept here, unlike text-field: a 20px box has no room for a flush ring to read as separate from its own border",
          "opacity (aria-disabled)": "state.disabled-opacity",
          "transition": "motion.interactive.duration with motion.interactive.easing",
          "border-radius": "radius.pill — a circle, which is the one thing that tells a radio from a checkbox before either is read",
          "checked mark": "theme.fg.on-accent as a centred dot; a radio is filled, not ticked"
        }
      }
    },
    "example": "<Radio value=\"annual\" label=\"Annual\" />"
  },
  {
    "name": "radio-group",
    "title": "Radio Group",
    "description": "A set of radios holding one answer, with the group owning the name, the value and the focus model.",
    "family": "inputs",
    "tier": "free",
    "status": {
      "css": {
        "state": "latest",
        "version": "0.1.0"
      },
      "react": {
        "state": "latest",
        "version": "0.1.0"
      },
      "figma": {
        "state": "future"
      }
    },
    "dependencies": [
      "radio",
      "state-layer"
    ],
    "importPath": "@rata/react",
    "mode": "documented",
    "note": null,
    "extends": "Omit<HTMLAttributes<HTMLDivElement>, \"role\" | \"onChange\" | \"defaultValue\">",
    "props": [
      {
        "name": "label",
        "optional": true,
        "type": "string",
        "description": "Accessible name for the group — the question these radios answer.",
        "declared": true,
        "summary": "Accessible name for the group — the question these radios answer.",
        "use": [
          "Always, unless `labelledBy` points at a visible heading that already asks it."
        ],
        "dont": [
          "Leaving both this and `labelledBy` unset; an unnamed radiogroup announces its options with no idea what they are options for."
        ],
        "conflicts": [
          "labelledBy"
        ],
        "a11y": "Becomes `aria-label` on the group."
      },
      {
        "name": "labelledBy",
        "optional": true,
        "type": "string",
        "description": "Id of a visible element that already names this group.",
        "declared": true,
        "summary": "Id of a visible element that already names this group.",
        "use": [],
        "dont": [],
        "conflicts": [
          "label"
        ],
        "a11y": null
      },
      {
        "name": "value",
        "optional": true,
        "type": "string | null",
        "description": "The chosen value, or null for nothing chosen yet.",
        "declared": true,
        "summary": "The chosen value, or null for nothing chosen yet.",
        "use": [],
        "dont": [
          "Passing it without `onValueChange` — the group would render a choice and refuse to change it."
        ],
        "conflicts": [
          "defaultValue"
        ],
        "a11y": null
      },
      {
        "name": "defaultValue",
        "optional": true,
        "type": "string | null",
        "description": "Starting choice for an uncontrolled group. Conflicts with `value`.",
        "declared": true,
        "summary": "Starting choice for an uncontrolled group.",
        "use": [],
        "dont": [],
        "conflicts": [
          "value"
        ],
        "a11y": null
      },
      {
        "name": "onValueChange",
        "optional": true,
        "type": "(value: string) => void",
        "description": "Called with the value the group should move to.",
        "declared": true,
        "summary": "Called with the value the group should move to.",
        "use": [],
        "dont": [
          "Assuming it only fires on click; arrow keys select as they move, which is the pattern working correctly."
        ],
        "conflicts": [],
        "a11y": null
      },
      {
        "name": "name",
        "optional": true,
        "type": "string",
        "description": "Shared form field name. Defaults to a generated one.",
        "declared": true,
        "summary": "Form field name shared by every radio. Defaults to a generated one.",
        "use": [
          "A real form post, where the name is the key the server reads."
        ],
        "dont": [],
        "conflicts": [],
        "a11y": null
      },
      {
        "name": "orientation",
        "optional": true,
        "type": "ButtonGroupOrientation",
        "description": null,
        "default": "\"vertical\"",
        "declared": true,
        "summary": "Which way the options stack, and which arrows move between them.",
        "use": [
          "`vertical` for a list of options, which is the default and the readable shape for anything with more than two."
        ],
        "dont": [],
        "conflicts": [],
        "a11y": "Announced as `aria-orientation`. Both axes' arrow keys still move, so a user who reaches for Down in a horizontal group does not hit a dead end."
      },
      {
        "name": "disabled",
        "optional": true,
        "type": "boolean",
        "description": "Blocks the whole question while keeping every option focusable.",
        "declared": true,
        "summary": "Blocks the whole question while keeping every option focusable and readable.",
        "use": [],
        "dont": [
          "Disabling one option; that belongs on the radio itself."
        ],
        "conflicts": [],
        "a11y": "Emits `aria-disabled` on each option, never the native `disabled` attribute."
      },
      {
        "name": "required",
        "optional": true,
        "type": "boolean",
        "description": "Marks the group as requiring an answer.",
        "declared": true,
        "summary": "Marks the group as requiring an answer.",
        "use": [],
        "dont": [],
        "conflicts": [],
        "a11y": "Emits `aria-required` on the group."
      }
    ],
    "usage": [
      {
        "case": "Choosing one of a few",
        "when": "The default: a short list of mutually exclusive options.",
        "example": "<RadioGroup label=\"Billing period\" value={period} onValueChange={setPeriod}>\n  <Radio value=\"monthly\" label=\"Monthly\" />\n  <Radio value=\"annual\" label=\"Annual\" description=\"Two months free\" />\n</RadioGroup>",
        "notes": "One tab stop for the whole group; arrows move between options and select as they go. That is the radio pattern, and it is why a radio group is not just several checkboxes."
      },
      {
        "case": "An option that cannot be chosen yet",
        "when": "One option unavailable while the rest remain answerable.",
        "example": "<RadioGroup label=\"Shipping\" value={ship} onValueChange={setShip}>\n  <Radio value=\"standard\" label=\"Standard\" />\n  <Radio value=\"overnight\" label=\"Overnight\" disabled description=\"Not available to this address\" />\n</RadioGroup>",
        "notes": "The disabled option still takes focus, so a keyboard user discovers it exists and reads why. Arrow keys land on it without selecting it."
      },
      {
        "case": "Reaching for the wrong component",
        "when": "The options are a filter that should be clearable, or a view switch.",
        "example": "<RadioGroup label=\"Status\" value={status} onValueChange={setStatus}>\n  <Radio value=\"open\" label=\"Open\" />\n</RadioGroup>",
        "notes": "Don't. A radio group cannot go back to nothing once answered — that is the platform's behaviour and users rely on it. A clearable set of options is toggle-button-group with `deselectable`; a view switch is a tab list, which this system does not have yet."
      }
    ],
    "behavior": {
      "primitive": "getRadioGroupProps",
      "summary": "The radiogroup pattern: one tab stop, arrow keys that move and select, and a single value owned here rather than by any item. Native inputs carry the semantics; this decides which one is checked and which one is the tab stop.",
      "decisions": [
        {
          "decision": "The group owns the value, not the items",
          "why": "A radio's whole meaning is 'one of these'. Leaving the value on the item would let two claim to be checked, which is a state the control is supposed to make impossible."
        },
        {
          "decision": "Native input[type=radio] sharing one `name`",
          "why": "Grouping, arrow-key behaviour and form submission all come from the platform when the name is shared. Re-implementing them on divs is how a radio group ends up not announcing how many options it has."
        },
        {
          "decision": "Roving tabindex is derived from the value",
          "why": "The checked radio is the tab stop; a group with nothing chosen is entered at its first enabled option. Deriving it means focus and value cannot disagree, because there is no second piece of state."
        },
        {
          "decision": "Arrow keys select as they move, and a disabled option only takes focus",
          "why": "Selecting on arrow is the radio pattern. This system's disabled controls stay focusable and announced, so a disabled radio must still be reachable — it just must not become the answer."
        },
        {
          "decision": "No deselect",
          "why": "Unlike toggle-button-group, a radio group has no way back to nothing once answered. That is the platform's behaviour and users rely on it; a clearable set of options is a different control, and this system has toggle-button-group for it."
        }
      ]
    },
    "tokens": {
      "vertical": {
        "summary": "The default: a readable column of options.",
        "tokens": {
          "gap between options": "space.stack.xs",
          "gap between group label and options": "space.stack.2xs",
          "group label color": "theme.fg.primary",
          "group label font-size": "type.label.size",
          "group label font-weight": "type.label.weight"
        }
      },
      "horizontal": {
        "summary": "A row, for two or three short options that fit on one line.",
        "tokens": {
          "gap between options": "space.gap.lg — wider than the vertical step, so two options on one line do not read as one phrase",
          "gap between group label and options": "space.stack.2xs",
          "group label color": "theme.fg.primary",
          "group label font-size": "type.label.size",
          "group label font-weight": "type.label.weight"
        }
      }
    },
    "example": "<RadioGroup label=\"Shipping\" defaultValue=\"standard\">\n        <Radio value=\"standard\" label=\"Standard\" />\n        <Radio\n          value=\"overnight\"\n          label=\"Overnight\"\n          disabled\n          description=\"Not available to this address\"\n        />\n      </RadioGroup>"
  },
  {
    "name": "spinner",
    "title": "Loading: Spinner",
    "description": "Inline loading indicator that scales with font size.",
    "family": "loading",
    "tier": "free",
    "status": {
      "css": {
        "state": "latest",
        "version": "0.1.0"
      },
      "react": {
        "state": "latest",
        "version": "0.1.0"
      },
      "figma": {
        "state": "future"
      }
    },
    "dependencies": [],
    "importPath": "@rata/react",
    "mode": "documented",
    "note": null,
    "extends": null,
    "props": [
      {
        "name": "label",
        "optional": true,
        "type": "string",
        "description": "Accessible label; omit when a parent already conveys the loading state.",
        "declared": true,
        "summary": "Accessible name for the loading state.",
        "use": [
          "A standalone spinner, where nothing else announces that something is loading."
        ],
        "dont": [
          "A spinner inside a Button — the button already carries `aria-busy` and its own label, so a second announcement is noise."
        ],
        "conflicts": [],
        "a11y": "Omitting this is correct only when a parent already conveys the loading state."
      }
    ],
    "usage": [
      {
        "case": "Standalone region loading",
        "when": "A panel or list fetching its first page.",
        "example": "<Spinner label=\"Loading results\" />"
      },
      {
        "case": "Inside a button",
        "when": "Composed automatically by Button's `loading` prop — you rarely write this yourself.",
        "example": "<Spinner />",
        "notes": "No label: Button's `aria-busy` already reports the state."
      }
    ],
    "behavior": null,
    "tokens": {},
    "example": "<Spinner label=\"Loading results\" />"
  },
  {
    "name": "state-layer",
    "title": "State layer",
    "description": "Composable hover/press overlay that gives every interactive component identical state feedback.",
    "family": "foundations",
    "tier": "free",
    "status": {
      "css": {
        "state": "latest",
        "version": "0.1.0"
      },
      "react": {
        "state": "na"
      },
      "figma": {
        "state": "future"
      }
    },
    "dependencies": [],
    "importPath": "@rata/react",
    "mode": "css-only",
    "note": "No standalone React component — \"State layer\" is CSS-only (see registry description), composed onto other components' className.",
    "extends": null,
    "props": [],
    "usage": [
      {
        "case": "Giving a new interactive component hover and press feedback",
        "when": "Always — this is the only supported way to express interaction feedback in this system.",
        "example": "<button className=\"rata-thing rata-state-layer\">…</button>",
        "notes": "Add the class alongside the component's own; never write a competing `:hover` background rule. Feedback here is opacity-only by standing decision, which is why the role tokens carry no `bg-hover`/`bg-active` and why `ring.neutral` is deliberately unwired."
      },
      {
        "case": "A host with no border",
        "when": "The layer's default bleeds outward by border.default, so an unbordered host needs the opt-out or its tint sits a hairline proud of the element.",
        "example": "<button className=\"rata-thing rata-state-layer rata-state-layer--flush\">…</button>",
        "notes": "The layer is an absolutely positioned pseudo-element, and `inset` on one resolves against the containing block's padding box — which excludes the border. At `inset: 0` the tint stopped a border-width short on every side, invisible under a filled background but plainly smaller on a host whose only fill is the layer itself. It now bleeds by border.default, since every bordered host in this system uses that width, and CSS gives no way to read a containing block's computed border-width. `--flush` restores `inset: 0` for hosts with none."
      }
    ],
    "behavior": null,
    "tokens": {},
    "example": null
  },
  {
    "name": "switch",
    "title": "Switch",
    "description": "An on/off setting that takes effect the moment it moves.",
    "family": "inputs",
    "tier": "free",
    "status": {
      "css": {
        "state": "latest",
        "version": "0.1.0"
      },
      "react": {
        "state": "latest",
        "version": "0.1.0"
      },
      "figma": {
        "state": "future"
      }
    },
    "dependencies": [
      "state-layer"
    ],
    "importPath": "@rata/react",
    "mode": "documented",
    "note": null,
    "extends": "Omit<",
    "props": [
      {
        "name": "label",
        "optional": false,
        "type": "ReactNode",
        "description": "The setting's label. Always required — `labelHidden` hides it, nothing removes it.",
        "declared": true,
        "summary": "The setting's label. Always required — `labelHidden` can take it off screen, but nothing removes it.",
        "use": [
          "Always. Name the setting, not the action: \"Notifications\", not \"Turn on notifications\"."
        ],
        "dont": [
          "A label that changes with the state. `aria-checked` says which way it is set, so a label that also says it produces \"Turn off notifications, on\".",
          "Passing an empty string. An unnamed switch is a control nobody can describe; hide it with `labelHidden` instead."
        ],
        "conflicts": [],
        "a11y": "A real `<label for>` bound to the input, so the text is part of the hit target."
      },
      {
        "name": "labelHidden",
        "optional": true,
        "type": "boolean",
        "description": "Takes the label off screen while leaving it in the accessibility tree.",
        "declared": true,
        "summary": "Takes the label off screen while leaving it in the accessibility tree.",
        "use": [
          "A row in a settings table whose column already names the setting."
        ],
        "dont": [
          "A standalone switch. With nothing else naming it, the label is the only thing saying what is being turned on."
        ],
        "conflicts": [],
        "a11y": "Clipped, not `display: none` — the label element is still rendered and still bound, so the accessible name is unchanged."
      },
      {
        "name": "checked",
        "optional": true,
        "type": "boolean",
        "description": "Whether the setting is on. Makes the component controlled.",
        "declared": true,
        "summary": "Whether the setting is on. Makes the component controlled.",
        "use": [
          "Whenever the setting lives outside the component, which for a setting is essentially always."
        ],
        "dont": [
          "Passing it without `onCheckedChange`; the switch would render its state and refuse to change it.",
          "Flipping it optimistically when the change is a server round trip that can fail — the switch would be lying about the setting."
        ],
        "conflicts": [
          "defaultChecked"
        ],
        "a11y": "Drives `aria-checked`, which is announced as on/off rather than checked/unchecked."
      },
      {
        "name": "defaultChecked",
        "optional": true,
        "type": "boolean",
        "description": "Starting state for an uncontrolled switch. Conflicts with `checked`.",
        "declared": true,
        "summary": "Starting state for an uncontrolled switch.",
        "use": [
          "A local view preference nothing else on the page reads."
        ],
        "dont": [],
        "conflicts": [
          "checked"
        ],
        "a11y": null
      },
      {
        "name": "onCheckedChange",
        "optional": true,
        "type": "(checked: boolean, event: { preventDefault(): void }) => void",
        "description": "Called with the state the setting should move to.",
        "declared": true,
        "summary": "Called with the state the setting should move to.",
        "use": [
          "Applying the change immediately — that is what makes this a switch rather than a checkbox."
        ],
        "dont": [
          "Deferring the effect to a Save button. If the change needs saving, the control is a Checkbox."
        ],
        "conflicts": [],
        "a11y": null
      },
      {
        "name": "description",
        "optional": true,
        "type": "ReactNode",
        "description": "Helper text under the label.",
        "declared": true,
        "summary": "Helper text under the label, for a setting whose consequence is not obvious.",
        "use": [
          "Saying what turning it on actually does, when the label alone cannot."
        ],
        "dont": [
          "Restating the label in more words."
        ],
        "conflicts": [],
        "a11y": null
      },
      {
        "name": "id",
        "optional": true,
        "type": "string",
        "description": "Stable id for the input. Defaults to a generated one.",
        "declared": true,
        "summary": "Stable id for the input. Defaults to a generated one.",
        "use": [],
        "dont": [],
        "conflicts": [],
        "a11y": null
      },
      {
        "name": "disabled",
        "optional": true,
        "type": "boolean",
        "description": "Blocks the change while keeping the switch focusable and readable.",
        "declared": true,
        "summary": "Blocks the change while keeping the switch focusable and readable.",
        "use": [
          "A setting that cannot be changed yet but whose current state still matters to see."
        ],
        "dont": [
          "Expressing \"off\". Disabled and off are different facts, and a disabled switch can be either."
        ],
        "conflicts": [],
        "a11y": "Emits `aria-disabled` and refuses activation. Never the native `disabled` attribute."
      },
      {
        "name": "className",
        "optional": true,
        "type": "string",
        "description": null,
        "declared": true,
        "summary": "Class for the switch's wrapper.",
        "use": [],
        "dont": [
          "Restyling the track or thumb — those are the approved token mapping."
        ],
        "conflicts": [],
        "a11y": null
      }
    ],
    "usage": [
      {
        "case": "A setting that applies immediately",
        "when": "The canonical case, and the only one this component is for.",
        "example": "<Switch\n  label=\"Notifications\"\n  checked={notifications}\n  onCheckedChange={setNotifications}\n/>",
        "notes": "The label names the setting, not the action. `aria-checked` says which way it is set, so \"Notifications\" is announced as \"Notifications, on\" — which is what a setting sounds like."
      },
      {
        "case": "A setting whose consequence needs saying",
        "when": "Where the label alone cannot explain what turning it on does.",
        "example": "<Switch\n  label=\"Public profile\"\n  description=\"Anyone with the link can see your activity.\"\n  checked={isPublic}\n  onCheckedChange={setPublic}\n/>"
      },
      {
        "case": "A row in a settings table",
        "when": "Where the column heading already names the setting.",
        "example": "<Switch label=\"Email digest\" labelHidden checked={row.digest} onCheckedChange={…} />",
        "notes": "`labelHidden` hides the pixels, not the name — each row is still announced with what it controls."
      },
      {
        "case": "Reaching for the wrong component",
        "when": "The change is not applied until a form is submitted.",
        "example": "<Switch label=\"Email me updates\" checked={draft.email} onCheckedChange={setDraft} />\n<Button>Save changes</Button>",
        "notes": "Don't. A switch beside a Save button promises something it does not do: it says the setting took effect, and it did not. That is a Checkbox — a value you are about to submit. The distinction is the entire reason both components exist."
      }
    ],
    "behavior": {
      "primitive": "getSwitchProps",
      "summary": "A native input[type=checkbox] carrying role=\"switch\". The platform supplies Space-to-toggle, the checked state and form participation; the role changes only how it is announced — \"Notifications, on\" rather than \"Notifications, checked\".",
      "decisions": [
        {
          "decision": "A setting, not a value waiting to be submitted",
          "why": "This is the whole reason the component exists separately from Checkbox. A checkbox is a value you are about to submit with a form; a switch takes effect the moment it moves. Getting this wrong is worse than a styling mistake: a switch beside a Save button promises something the control does not do."
        },
        {
          "decision": "role=\"switch\" on a native checkbox, not a div",
          "why": "The role is the only thing that has to change. Rebuilding the keyboard handling, the checked state and the form participation on a div is how a switch ends up not announcing its state, and there is nothing to gain from it."
        },
        {
          "decision": "No indeterminate state",
          "why": "A setting is on or off. \"Partly on\" is not a state a system can be in — a parent summarising children that disagree is a Checkbox, which has exactly that state for exactly that reason."
        },
        {
          "decision": "The label does not change with the state",
          "why": "aria-checked already says which way it is set. Swapping the label to \"Turn off notifications\" means the name and the state contradict each other, and a screen reader announces \"Turn off notifications, on\" — a double negative the user has to unpick. The same rule toggle-button follows."
        },
        {
          "decision": "Activation is guarded, not disabled",
          "why": "As everywhere in this system: aria-disabled plus a refused activation, so the switch stays focusable and keeps announcing which way it is set. readOnly is no help — it has no effect on a checkbox."
        }
      ]
    },
    "tokens": {
      "base": {
        "summary": "A track the thumb slides along. The track's fill is the state, and the thumb's position says the same thing a second way, so the setting is never carried by colour alone.",
        "tokens": {
          "track inline-size": "size.control.lg — the track is twice the thumb's diameter plus its inset, which is what gives the thumb somewhere to travel; borrowing a control step keeps it on the scale",
          "track block-size": "size.icon.md — the same height as a checkbox's box, so a switch and a checkbox in one settings column line up",
          "track border-radius": "radius.pill",
          "track background (off)": "theme.bg.muted — the most recessed surface, so an off switch reads as an empty channel rather than a filled control",
          "track background (on)": "theme.accent-role.bg",
          "track border": "border.default solid theme.border.strong when off — an off track needs its own boundary at the 3:1 WCAG 1.4.11 asks of a control's edge, because its fill is nearly its surroundings. On, the accent fill is the boundary and the border matches it",
          "thumb size": "space.gap.lg — a spacing step rather than the icon scale, because the thumb is a mark and not a glyph, the same reasoning the radio's dot follows. At 16px it fills 89% of the track's inner height, which is the proportion a switch reads as; smaller and it looks like a dot rattling in a channel",
          "thumb border-radius": "radius.pill",
          "thumb background (off)": "theme.fg.secondary",
          "thumb background (on)": "theme.fg.on-accent — the only foreground permitted on the accent fill, and it inverts with the accent in the dark scheme",
          "thumb inset": "space.0-5 at both ends of the travel — tighter than the checkbox's inset on purpose, since a large thumb wants a couple of pixels of channel showing rather than a gutter",
          "thumb travel": "transform, not a layout property: the thumb slides rather than reflowing, so nothing around it moves",
          "gap between track and label": "space.gap.sm",
          "gap between label and description": "space.stack.2xs",
          "label color": "theme.fg.primary",
          "label font-size": "type.control.size.md",
          "description color": "theme.fg.secondary",
          "description font-size": "type.supporting.size",
          "hover/press": "compose the .rata-state-layer class",
          "outline (focus-visible)": "focus.ring-width solid theme.focus-ring, offset focus.ring-offset — drawn on the track, and the offset is kept: a pill this small has no room for a flush ring to read as separate from its own edge",
          "opacity (aria-disabled)": "state.disabled-opacity",
          "transition": "motion.interactive.duration with motion.interactive.easing, on the thumb's transform and the track's fill"
        }
      }
    },
    "example": "<Switch label=\"Push digest\" labelHidden />"
  },
  {
    "name": "text-field",
    "title": "Input: Text field",
    "description": "Single-line text input with label, helper text, and validation states.",
    "family": "inputs",
    "tier": "free",
    "status": {
      "css": {
        "state": "latest",
        "version": "0.1.0"
      },
      "react": {
        "state": "latest",
        "version": "0.1.0"
      },
      "figma": {
        "state": "future"
      }
    },
    "dependencies": [],
    "importPath": "@rata/react",
    "mode": "documented",
    "note": null,
    "extends": "Omit<",
    "props": [
      {
        "name": "label",
        "optional": false,
        "type": "ReactNode",
        "description": "The field's label. Always required — `labelHidden` hides it, nothing removes it.",
        "declared": true,
        "summary": "The field's label. Always required — `labelHidden` can take it off screen, but nothing removes it.",
        "use": [
          "Always. Every field has a label, whether or not it is visible."
        ],
        "dont": [
          "Placeholder-as-label — the placeholder disappears on first keystroke, taking the field's name with it. `labelHidden` exists so the visual choice never costs the name.",
          "Passing an empty string. An empty label is an unnamed field, which is the one thing this component will not render; hide it with `labelHidden` instead."
        ],
        "conflicts": [],
        "a11y": "Rendered as a real `<label for>` bound to the input's id, so clicking it focuses the field. Still a real label when hidden — only its pixels go away."
      },
      {
        "name": "labelHidden",
        "optional": true,
        "type": "boolean",
        "description": "Takes the label off screen while leaving it in the accessibility tree.",
        "declared": true,
        "summary": "Takes the label off screen while leaving it in the accessibility tree.",
        "use": [
          "A field whose purpose is already unambiguous on screen — a search input beside a magnifier icon, a filter in a toolbar column with its own heading.",
          "A dense row of fields under a table header that names each column, where repeating the name per row would be noise."
        ],
        "dont": [
          "Hiding a label because the form looks crowded. The label is the last thing to cut; if space is short the layout budget is wrong.",
          "Hiding it and then relying on `placeholder` to say what the field is — the placeholder is gone the moment someone types, and this prop exists precisely so that trade is never necessary.",
          "Hiding it when the field's only other context is its position. Position is not a name, and it is the first thing lost when a layout reflows."
        ],
        "conflicts": [],
        "a11y": "The label element is still rendered and still bound by `for`, so the accessible name is unchanged — it is clipped, not removed. It is not `display: none`, which would take the name with it. The required marker goes off screen with it; `aria-required` still carries that."
      },
      {
        "name": "id",
        "optional": true,
        "type": "string",
        "description": "Stable id for the input. Defaults to a generated one.",
        "declared": true,
        "summary": "Stable id for the input. Defaults to a generated one.",
        "use": [
          "Linking an external `<label>` or an outside `aria-describedby` to this field.",
          "Deep-linking or scroll-to-error, where the id has to be predictable across renders."
        ],
        "dont": [
          "Reusing one id across two fields — the derived description and message ids would collide too."
        ],
        "conflicts": [],
        "a11y": null
      },
      {
        "name": "size",
        "optional": true,
        "type": "TextFieldSize",
        "description": "Control height and inline padding. Text size does not change with it.",
        "default": "\"md\"",
        "values": [
          "sm",
          "md",
          "lg"
        ],
        "declared": true,
        "summary": "Control height and inline padding, from the `size.control.*` scale. Text size does not change with it.",
        "use": [
          "`md` everywhere by default.",
          "`sm` inside dense rows, table cells, and filter bars.",
          "`lg` for a single prominent field on a sparse surface — a sign-in form, an empty-state search."
        ],
        "dont": [
          "Shrinking to `sm` to fit a cramped layout — fix the layout budget instead.",
          "Expecting the text to grow with it. All three sizes set the same text size, so `size` buys a bigger target for the same field rather than a louder one — the same bargain `Button` makes, which is what lets a field and a button sit in one row and measure alike.",
          "Mixing sizes down a single form. The fields are one set, so they measure as one set.",
          "Setting height or padding yourself; the size prop is the only supported way to change either.",
          "Reaching for the native HTML `size` attribute — it is deliberately shadowed by this prop. It measures a visible width in characters, a pre-CSS legacy, and width here belongs to the form via `className`."
        ],
        "conflicts": [],
        "a11y": "Visual only. A larger control is a larger pointer target, which is a usability gain rather than an accessibility claim — none of the three sizes falls below the system's control heights."
      },
      {
        "name": "status",
        "optional": true,
        "type": "TextFieldStatus",
        "description": "Validation lifecycle. Drives `aria-invalid`, `aria-busy`, and the ring.",
        "default": "\"idle\"",
        "values": [
          "idle",
          "validating",
          "valid",
          "invalid"
        ],
        "declared": true,
        "summary": "Validation lifecycle. Drives `aria-invalid`, `aria-busy`, and which ring token the field wears.",
        "use": [
          "`validating` for an in-flight async check, such as a uniqueness lookup.",
          "`invalid` once a check has actually failed, paired with a `message` saying how to fix it.",
          "`valid` only where confirmation carries information — a passing async check, not every filled field."
        ],
        "dont": [
          "Deriving it from whether `message` is non-empty — `idle` and `valid` are different states and must stay distinguishable.",
          "`invalid` while the user is still typing their first attempt; validate on blur or submit."
        ],
        "conflicts": [],
        "a11y": "`invalid` sets `aria-invalid`; `validating` sets `aria-busy`. Neither is conveyed by the ring colour alone, which is why the `message` is not optional in those states."
      },
      {
        "name": "description",
        "optional": true,
        "type": "ReactNode",
        "description": "Persistent helper text under the input.",
        "declared": true,
        "summary": "Persistent helper text under the input — format hints, constraints, why the field is asked for.",
        "use": [
          "Stating a constraint before it is violated, which is cheaper than an error after."
        ],
        "dont": [
          "Error text — that is `message`, which is announced and cleared with the status.",
          "Text that only matters once, which belongs in the surrounding form copy."
        ],
        "conflicts": [],
        "a11y": "Always in `aria-describedby`, in front of the message, so the constraint is heard before the failure."
      },
      {
        "name": "message",
        "optional": true,
        "type": "ReactNode",
        "description": "Status-dependent message under the input: the error, confirmation, or in-flight note.",
        "declared": true,
        "summary": "Status-dependent message under the input: the error, the confirmation, or the in-flight note.",
        "use": [
          "Saying how to fix an `invalid` field, in the user's terms rather than the validator's."
        ],
        "dont": [
          "Leaving a stale message when the status returns to `idle` — it stays rendered but drops out of the description chain, which reads as a live region emptying.",
          "Restating the label ('Email is invalid') where the fix ('Use the format name@example.com') would help."
        ],
        "conflicts": [],
        "a11y": "Announced politely, and joins `aria-describedby` only while the status is not `idle`."
      },
      {
        "name": "disabled",
        "optional": true,
        "type": "boolean",
        "description": "Blocks editing while keeping the field focusable, readable and announced.",
        "declared": true,
        "summary": "Blocks editing while keeping the field focusable, readable, and announced.",
        "use": [
          "A field temporarily unavailable but still meaningful to read — awaiting a prior answer, say."
        ],
        "dont": [
          "Fields you need excluded from form submission: this emits `readOnly`, and a readOnly field still submits.",
          "Permanently uneditable values, which should be text rather than a dead input."
        ],
        "conflicts": [],
        "a11y": "Emits `aria-disabled` plus `readOnly`, never the native `disabled` attribute."
      },
      {
        "name": "required",
        "optional": true,
        "type": "boolean",
        "description": "Marks the field required to assistive technology and in the label.",
        "declared": true,
        "summary": "Marks the field as required to assistive technology and in the label.",
        "use": [
          "Fields the form genuinely cannot be submitted without."
        ],
        "dont": [
          "Marking every field required, which makes the marker carry no information.",
          "Relying on it for validation — it announces the requirement, it does not enforce it."
        ],
        "conflicts": [],
        "a11y": "Emits `aria-required`. The visible required marker is the label's job, not the attribute's."
      },
      {
        "name": "className",
        "optional": true,
        "type": "string",
        "description": "Class for the wrapper. The input itself is styled by the system.",
        "declared": true,
        "summary": "Class for the field's wrapper — the label, input and helper text are styled by the system.",
        "use": [
          "Placing the field in a form's layout: a column span, a width, a margin token the parent cannot express."
        ],
        "dont": [
          "Restyling the input through a descendant selector. The border, ring and height are the approved token mapping, and overriding them here puts a second answer in a place the contract cannot see.",
          "Setting a width on the wrapper to imply a maximum input length — that is a `maxLength` and, better, a `description` stating the constraint before it is violated."
        ],
        "conflicts": [],
        "a11y": null
      }
    ],
    "usage": [
      {
        "case": "Plain labelled field with a format hint",
        "when": "The default case. Helper text states the constraint before it can be violated.",
        "example": "<TextField label=\"Email\" description=\"We'll only use this for receipts.\" />"
      },
      {
        "case": "Failed validation after blur",
        "when": "A check has run and failed. The message says how to fix it, not merely that it is wrong.",
        "example": "<TextField\n  label=\"Email\"\n  status=\"invalid\"\n  message=\"Use the format name@example.com\"\n/>",
        "notes": "`aria-invalid` is set and the message joins the description chain; the danger ring is the visual half of the same signal, never the whole of it."
      },
      {
        "case": "Async uniqueness check in flight",
        "when": "A server-side check is running against what the user typed.",
        "example": "<TextField\n  label=\"Workspace URL\"\n  status=\"validating\"\n  message=\"Checking availability…\"\n/>",
        "notes": "`validating` is for a check on this field's own value — not for a pending form submit, which belongs to the submit button's `loading`."
      },
      {
        "case": "Unavailable but readable",
        "when": "The field depends on an answer the user has not given yet.",
        "example": "<TextField\n  label=\"State\"\n  disabled\n  description=\"Choose a country first.\"\n/>",
        "notes": "Stays focusable and announced. Remember it still submits its value — clear it yourself if that matters."
      },
      {
        "case": "Purpose already clear on screen",
        "when": "A search input beside a magnifier, or a column of fields under a header that names them — where a visible label per field would be noise.",
        "example": "<TextField label=\"Search\" labelHidden placeholder=\"Search…\" />",
        "notes": "`labelHidden` hides the pixels, not the name: the `<label for>` is still rendered and still bound, so the field is announced as \"Search\" exactly as it would be with the label visible. Note the `placeholder` is a convenience here, not the name — it disappears on the first keystroke, which is why the label still has to exist."
      }
    ],
    "behavior": {
      "primitive": "getTextFieldProps",
      "summary": "Label/description/message association, validation semantics, and the disabled contract — independent of React and of any styling. The caller owns the `id`; the description and message ids are derived from it, so association cannot be hand-wired wrong.",
      "decisions": [
        {
          "decision": "`readOnly` is the disabled mechanism, not the native `disabled` attribute",
          "why": "Consistent with the system-wide rule that disabled controls stay focusable and screen-reader discoverable. A button blocks activation in its click guard; an input has no activation to guard, so `readOnly` is the analogue. Consequence to know: a readOnly field still submits its value, where a disabled one does not."
        },
        {
          "decision": "One four-state `status` enum rather than separate booleans",
          "why": "`idle | validating | valid | invalid` maps one-to-one onto the four ring mappings in the token recipe, and drives `aria-invalid` and `aria-busy`. Separate `invalid`/`validating` booleans would make contradictory combinations expressible."
        },
        {
          "decision": "The message is `aria-live=\"polite\"`, not `role=\"alert\"`",
          "why": "The message is already in `aria-describedby` whenever the status is not idle, so an assertive live region would announce it twice — once on change, once when focus lands. The tradeoff accepted: an error appearing while focus is elsewhere is announced at the next pause rather than immediately."
        },
        {
          "decision": "The message joins `aria-describedby` only once the status leaves `idle`",
          "why": "An empty or stale message element in the description chain makes every field announce trailing silence, and `idle` is a real state distinct from `valid` — not merely 'no error yet'."
        }
      ]
    },
    "tokens": {
      "base": {
        "summary": "A single-line input with a label, helper text, and validation states.",
        "tokens": {
          "label color": "theme.fg.primary",
          "label font-size": "type.label.size",
          "input background": "theme.bg.surface",
          "input color": "theme.fg.primary",
          "input font-family": "font.family.sans",
          "input font-size": "type.control.size.md — constant across all three sizes, so `size` changes the box and not the text. The same constant the button family uses, so a field and a button in one row measure their text alike",
          "input border-color": "theme.border.strong",
          "placeholder color": "theme.fg.muted",
          "height": "size.control.md — and size.control.sm / .lg at the other sizes. Together with padding-inline this is the only thing `size` changes",
          "padding-inline": "space.control.padding-inline.md — and the sm / lg steps at the other sizes",
          "border-radius": "radius.element",
          "helper text color": "theme.fg.secondary",
          "error text color": "theme.danger-role.fg",
          "error border-color": "theme.danger-role.fg",
          "validating (in-progress) ring": "theme.accent-role.ring",
          "valid ring": "theme.success-role.ring",
          "invalid ring": "theme.danger-role.ring",
          "gap between label, input, and helper": "space.stack.2xs — the tightest vertical step. The text either side carries line-height leading of its own, so this reads looser than 4px and space.stack.xs read as separation",
          "required marker color": "theme.fg.secondary — required-ness is information, not a fault, so it is deliberately not the danger role's foreground; the requirement itself is announced through aria-required",
          "disabled opacity": "state.disabled-opacity on the root — the field stays readable and focusable, which is why it emits aria-disabled and readOnly rather than the native disabled attribute",
          "hover inner band": "theme.bg.muted at border.2, drawn as an inset shadow so the fill recedes 2px while the border stays border.1 and the outer edge does not move. Idle only — the validation rings occupy the same 2px and box-shadow does not accumulate",
          "focus border-color": "theme.fg.primary — the same tone as the outline, so the two read as one stroke instead of a dark ring around a lighter edge. The width is never touched",
          "focus outline": "focus.ring-width solid theme.fg.primary, offset space.0 — deliberately NOT theme.focus-ring, the only such departure in the library. It gives 16.75:1 against the field's fill where theme.focus-ring gives 4.31:1, so the indicator is stronger; the cost is that focus here does not match focus on a Button. An outline rather than a thicker border because outlines are out of flow: this control sets height but not width, so a wider border would widen the field",
          "hidden label geometry": "border.1 box with space.0 padding and border, clipped — a 1px clipped box rather than display:none or a 0x0 one, because the first removes the accessible name and the second is skipped by some assistive tech. No colour or type token applies: the label is off screen, not restyled"
        }
      }
    },
    "example": "<TextField className=\"pg-field\" label=\"Search\" labelHidden placeholder=\"Search…\" />"
  },
  {
    "name": "toggle-button",
    "title": "Toggle Button",
    "description": "A button that stays on: an on/off state the user sets directly, announced with aria-pressed.",
    "family": "buttons",
    "tier": "free",
    "status": {
      "css": {
        "state": "latest",
        "version": "0.1.0"
      },
      "react": {
        "state": "latest",
        "version": "0.1.0"
      },
      "figma": {
        "state": "future"
      }
    },
    "dependencies": [
      "button",
      "state-layer",
      "spinner"
    ],
    "importPath": "@rata/react",
    "mode": "documented",
    "note": null,
    "extends": "Omit<ButtonHTMLAttributes<HTMLButtonElement>, \"disabled\" | \"onChange\">",
    "props": [
      {
        "name": "pressed",
        "optional": true,
        "type": "boolean",
        "description": "Whether the toggle is currently on. Makes the component controlled.",
        "declared": true,
        "summary": "Whether the toggle is currently on. Makes the component controlled.",
        "use": [
          "Whenever the state lives outside the component — which is nearly always, since a toggle usually reflects something a document or a preference already knows."
        ],
        "dont": [
          "Passing it without `onPressedChange`; the toggle would render its state and then refuse to ever change it.",
          "Deriving it from the last click rather than from the thing being toggled — if the underlying change fails, the button is left lying about it."
        ],
        "conflicts": [
          "defaultPressed"
        ],
        "a11y": "Becomes `aria-pressed`, emitted in both states. This is the only thing that tells assistive technology the control is a toggle at all."
      },
      {
        "name": "defaultPressed",
        "optional": true,
        "type": "boolean",
        "description": "Starting state for an uncontrolled toggle. Conflicts with `pressed`.",
        "declared": true,
        "summary": "Starting state for an uncontrolled toggle that owns its own value.",
        "use": [
          "Genuinely local state nothing else reads — a disclosure of extra columns in one table, say."
        ],
        "dont": [
          "Anything the rest of the page reacts to. The moment two places need the value, uncontrolled means they disagree.",
          "Resetting the toggle by changing this later — it is read once, at mount."
        ],
        "conflicts": [
          "pressed"
        ],
        "a11y": "No difference in what is announced: `aria-pressed` is emitted either way."
      },
      {
        "name": "onPressedChange",
        "optional": true,
        "type": "(pressed: boolean, event: MouseEvent<HTMLButtonElement>) => void",
        "description": "Called with the state the toggle should move to, not the one it is in.",
        "declared": true,
        "summary": "Called with the state the toggle should move to — not the one it is leaving.",
        "use": [
          "Applying the change, then letting the new state flow back in through `pressed`."
        ],
        "dont": [
          "Reading the current value off the event target; the argument already says where it is going.",
          "Treating it as a plain click handler — it does not fire when the toggle is disabled or loading."
        ],
        "conflicts": [],
        "a11y": "Fires for keyboard activation too, since it hangs off the same click guard the button uses. Space and Enter behave as they do on any native button."
      },
      {
        "name": "variant",
        "optional": true,
        "type": "ToggleButtonVariant",
        "description": "How loud the toggle is in its off state.",
        "default": "\"secondary\"",
        "values": [
          "secondary",
          "tertiary"
        ],
        "declared": true,
        "summary": "How loud the toggle is in its off state.",
        "use": [
          "`tertiary` for a toolbar of many toggles, where a border each would be visual noise.",
          "`secondary` for one or two toggles that need to read as controls on their own."
        ],
        "dont": [
          "Expecting a `primary` or `destructive` toggle. A filled accent control reads as \"the action to take\", which fights with \"a state you are in\", and a destructive thing you can leave switched on is a bug waiting to happen."
        ],
        "conflicts": [],
        "a11y": "Visual only. The pressed state must never be carried by colour alone — the on state changes fill *and* is announced through `aria-pressed`."
      },
      {
        "name": "size",
        "optional": true,
        "type": "ToggleButtonSize",
        "description": null,
        "default": "\"md\"",
        "values": [
          "sm",
          "md",
          "lg"
        ],
        "declared": true,
        "summary": "Control height and inline padding, from the `size.control.*` scale. Text size does not change with it.",
        "use": [
          "Matching the density of the surrounding controls, exactly as `Button` does — including that the label stays one size across all three."
        ],
        "dont": [
          "Mixing sizes within one group of toggles — they read as a set, so they measure as a set.",
          "Expecting an icon-only toggle's glyph to grow with it. The glyph follows the text size, which is constant, so only the box changes."
        ],
        "conflicts": [],
        "a11y": null
      },
      {
        "name": "disabled",
        "optional": true,
        "type": "boolean",
        "description": null,
        "declared": true,
        "summary": "Blocks activation while keeping the toggle focusable, announced, and readable in its current state.",
        "use": [
          "A toggle that cannot change yet but whose current state still matters to see."
        ],
        "dont": [
          "Expressing \"off\". Disabled and unpressed are different facts, and a disabled toggle can be either."
        ],
        "conflicts": [
          "loading"
        ],
        "a11y": "Emits `aria-disabled`, never the native `disabled` attribute: the control stays in the tab order and keeps announcing its pressed state."
      },
      {
        "name": "loading",
        "optional": true,
        "type": "boolean",
        "description": "Shows a spinner and blocks activation while preserving focus.",
        "declared": true,
        "summary": "Shows a spinner and refuses activation while the change this toggle asked for is still in flight.",
        "use": [
          "A toggle whose effect is a server round trip — starring, subscribing, publishing — where the state should not settle until the change lands."
        ],
        "dont": [
          "Flipping `pressed` optimistically *and* setting `loading`; pick one story about what the button currently means.",
          "Pairing it with `disabled` — `loading` already blocks activation, and the two together say the same thing twice."
        ],
        "conflicts": [
          "disabled"
        ],
        "a11y": "Emits `aria-busy` alongside `aria-disabled`, so the control is heard as busy rather than as broken. `aria-pressed` keeps reporting the state it is still in."
      },
      {
        "name": "iconOnly",
        "optional": true,
        "type": "boolean",
        "description": "Square icon-only toggle; pass the icon as children and set aria-label.",
        "declared": true,
        "summary": "Square toggle carrying an icon and no visible text.",
        "use": [
          "Toolbars where the icons are conventional and the row would otherwise be unreadably wide — bold, italic, alignment."
        ],
        "dont": [
          "Icons whose meaning is not already settled. An unlabelled toggle with an ambiguous glyph is a control nobody can describe to a colleague.",
          "Relying on the icon alone to show the pressed state — the fill must change too."
        ],
        "conflicts": [],
        "a11y": "You MUST pass `aria-label`. Nothing supplies an accessible name when there is no visible text, and the name must stay constant across states — the state is `aria-pressed`'s job, not the label's."
      }
    ],
    "usage": [
      {
        "case": "A formatting toggle in a toolbar",
        "when": "The canonical case: the state belongs to the document, so the toggle is controlled.",
        "example": "<ToggleButton\n  variant=\"tertiary\"\n  iconOnly\n  aria-label=\"Bold\"\n  pressed={isBold}\n  onPressedChange={setBold}\n>\n  B\n</ToggleButton>",
        "notes": "The label stays \"Bold\" in both states. `aria-pressed` says which state it is in — renaming it to \"Unbold\" would make the announcement contradict itself."
      },
      {
        "case": "A toggle whose change has to reach a server",
        "when": "Starring, subscribing, publishing — anything where the state should not settle until the write lands.",
        "example": "<ToggleButton pressed={starred} loading={saving} onPressedChange={star}>\n  Star\n</ToggleButton>",
        "notes": "`loading` blocks repeat activation on its own — don't also pass `disabled`. If the write fails, leave `pressed` where it was rather than flipping it back, which reads as the user having done something."
      },
      {
        "case": "Local-only state nothing else reads",
        "when": "The rare uncontrolled case: one view's own disclosure of extra detail.",
        "example": "<ToggleButton defaultPressed onPressedChange={setShowDetail}>\n  Show detail\n</ToggleButton>",
        "notes": "The moment a second place needs to know whether detail is showing, this becomes `pressed` — uncontrolled state that two components read is state that will disagree."
      },
      {
        "case": "Reaching for the wrong component",
        "when": "The control is a setting in a form, applied immediately, and reads as a thing with a state.",
        "example": "<ToggleButton pressed={notifications} onPressedChange={setNotifications}>\n  Notifications\n</ToggleButton>",
        "notes": "Don't. That is a switch, not a toggle button: it is a setting, not an action left engaged. Use Switch, which announces with aria-checked as \"Notifications, on\" — the sentence a setting should produce."
      }
    ],
    "behavior": {
      "primitive": "getToggleButtonProps",
      "summary": "Pressed-state semantics over the button's own activation contract. State is not held here: the caller owns `pressed` and is told what it should become, which is what lets a toggle inside a group and a toggle on its own be the same primitive.",
      "decisions": [
        {
          "decision": "Composes `getButtonProps` instead of reimplementing activation",
          "why": "A toggle is a button that carries a state. Implementing the disabled and loading guards a second time is how the two drift apart — one gains a fix the other doesn't, and the difference only shows up in whichever one nobody tested."
        },
        {
          "decision": "`aria-pressed` is emitted in both states, never omitted when false",
          "why": "Unlike `aria-disabled`, which is absent when it doesn't apply, a missing `aria-pressed` makes the control announce as an ordinary button — precisely when the user most needs to know it is a toggle that is currently off."
        },
        {
          "decision": "`aria-pressed`, not `role=\"switch\"`",
          "why": "A switch is an on/off setting that takes effect immediately and reads as a thing with a state (\"Notifications, on\"). A toggle button is an action you leave engaged — bold, mute, pin — and reads as a verb. When the control is a setting, the answer is Switch, and this component is the wrong one."
        },
        {
          "decision": "The label does not change with the state",
          "why": "`aria-pressed` already says whether it is on. Swapping the label to \"Unmute\" as well means the name and the state contradict each other — a screen reader announces \"Unmute, pressed\", which is a double negative the user has to unpick. The visual treatment carries the state; the word stays put."
        },
        {
          "decision": "No `data-pressed` attribute",
          "why": "`[aria-pressed=\"true\"]` is already a CSS selector. A parallel data attribute would be a second copy of the same fact, free to go stale."
        },
        {
          "decision": "Controlled by default, with an uncontrolled escape hatch",
          "why": "A toggle almost always reflects state that lives somewhere else — a document's formatting, a saved preference — so the controlled form is the honest default. `defaultPressed` exists for the genuinely local case, and passing both is a conflict rather than a precedence rule to memorize."
        }
      ]
    },
    "tokens": {
      "secondary": {
        "summary": "The off state of a toggle that has to read as a control on its own — identical to the secondary button, because an unpressed toggle is one.",
        "tokens": {
          "background": "theme.secondary-role.bg",
          "color": "theme.secondary-role.fg",
          "border-color": "theme.secondary-role.border",
          "height": "size.control.md — and size.control.sm / .lg at the other sizes; this is the only thing `size` changes",
          "padding-inline": "space.control.padding-inline.md",
          "gap": "space.gap.xs",
          "border-radius": "radius.element",
          "font-size": "type.control.size.md — constant across all three sizes, so `size` changes the box and not the label",
          "font-weight": "type.control.weight",
          "line-height": "type.control.line-height.md — constant, as with font-size",
          "transition-duration": "motion.interactive.duration",
          "transition-timing-function": "motion.interactive.easing",
          "outline (focus-visible)": "focus.ring-width solid theme.focus-ring, offset focus.ring-offset",
          "hover/press": "compose the .rata-state-layer class — do not swap the background",
          "opacity (aria-disabled)": "state.disabled-opacity"
        }
      },
      "secondary-pressed": {
        "summary": "The on state: a tint, not a fill. A saturated accent background reads as \"the action to take\", which is the wrong sentence for \"a state you are in\" — the same reason this component has no primary variant.",
        "tokens": {
          "background": "theme.accent-role.subtle",
          "color": "theme.accent-role.fg",
          "border-color": "theme.accent-role.fg — the border moves with the fill, so the pressed state is not carried by background alone",
          "height": "size.control.md — and size.control.sm / .lg at the other sizes; this is the only thing `size` changes",
          "padding-inline": "space.control.padding-inline.md",
          "gap": "space.gap.xs",
          "border-radius": "radius.element",
          "font-size": "type.control.size.md — constant across all three sizes, so `size` changes the box and not the label",
          "font-weight": "type.control.weight",
          "line-height": "type.control.line-height.md — constant, as with font-size",
          "transition-duration": "motion.interactive.duration",
          "transition-timing-function": "motion.interactive.easing",
          "outline (focus-visible)": "focus.ring-width solid theme.focus-ring, offset focus.ring-offset",
          "hover/press": "compose the .rata-state-layer class — do not swap the background",
          "opacity (aria-disabled)": "state.disabled-opacity"
        }
      },
      "tertiary": {
        "summary": "The off state for a toolbar of many toggles, where a border each would be visual noise.",
        "tokens": {
          "background": "none — no fill in the off state, so a row of toggles reads as one surface",
          "color": "theme.tertiary-role.fg",
          "border-color": "transparent — held so the box does not resize when the pressed state paints its border",
          "height": "size.control.md — and size.control.sm / .lg at the other sizes; this is the only thing `size` changes",
          "padding-inline": "space.control.padding-inline.md",
          "gap": "space.gap.xs",
          "border-radius": "radius.element",
          "font-size": "type.control.size.md — constant across all three sizes, so `size` changes the box and not the label",
          "font-weight": "type.control.weight",
          "line-height": "type.control.line-height.md — constant, as with font-size",
          "transition-duration": "motion.interactive.duration",
          "transition-timing-function": "motion.interactive.easing",
          "outline (focus-visible)": "focus.ring-width solid theme.focus-ring, offset focus.ring-offset",
          "hover/press": "compose the .rata-state-layer class — do not swap the background",
          "opacity (aria-disabled)": "state.disabled-opacity"
        }
      },
      "tertiary-pressed": {
        "summary": "The on state of a borderless toggle: the tint alone separates it from its unpressed neighbours.",
        "tokens": {
          "background": "theme.accent-role.subtle",
          "color": "theme.accent-role.fg",
          "border-color": "transparent — the tint is the cue; a border here would out-shout the secondary variant's pressed state",
          "height": "size.control.md — and size.control.sm / .lg at the other sizes; this is the only thing `size` changes",
          "padding-inline": "space.control.padding-inline.md",
          "gap": "space.gap.xs",
          "border-radius": "radius.element",
          "font-size": "type.control.size.md — constant across all three sizes, so `size` changes the box and not the label",
          "font-weight": "type.control.weight",
          "line-height": "type.control.line-height.md — constant, as with font-size",
          "transition-duration": "motion.interactive.duration",
          "transition-timing-function": "motion.interactive.easing",
          "outline (focus-visible)": "focus.ring-width solid theme.focus-ring, offset focus.ring-offset",
          "hover/press": "compose the .rata-state-layer class — do not swap the background",
          "opacity (aria-disabled)": "state.disabled-opacity"
        }
      }
    },
    "example": "<ToggleButton value=\"bold\">Bold</ToggleButton>"
  },
  {
    "name": "toggle-button-group",
    "title": "Toggle Button Group",
    "description": "A set of toggle buttons that holds a value: one choice out of several, or any number of independent states.",
    "family": "buttons",
    "tier": "free",
    "status": {
      "css": {
        "state": "latest",
        "version": "0.1.0"
      },
      "react": {
        "state": "latest",
        "version": "0.1.0"
      },
      "figma": {
        "state": "future"
      }
    },
    "dependencies": [
      "toggle-button",
      "button",
      "state-layer"
    ],
    "importPath": "@rata/react",
    "mode": "documented",
    "note": null,
    "extends": "Omit<HTMLAttributes<HTMLDivElement>, \"role\" | \"onChange\" | \"defaultValue\">",
    "props": [
      {
        "name": "value",
        "optional": true,
        "type": "ToggleButtonGroupValue",
        "description": "The current selection: a value in single mode, a list in multiple mode.",
        "declared": true,
        "summary": "The current selection: a value in single mode, a list in multiple mode, `null` or `[]` for none.",
        "use": [
          "Whenever the selection is state the rest of the page reads — which is most of the time, since a toggle group usually is the setting rather than a view of it."
        ],
        "dont": [
          "Passing a bare string in multiple mode, or an array in single mode; the shape follows `selectionMode` and mixing them makes the selected state silently wrong.",
          "Passing it without `onValueChange` — the group would render a selection and then refuse to change it."
        ],
        "conflicts": [
          "defaultValue"
        ],
        "a11y": "Drives `aria-checked` in single mode and `aria-pressed` in multiple mode. It is also what places the group's single tab stop in single mode."
      },
      {
        "name": "defaultValue",
        "optional": true,
        "type": "ToggleButtonGroupValue",
        "description": "Starting selection for an uncontrolled group. Conflicts with `value`.",
        "declared": true,
        "summary": "Starting selection for an uncontrolled group that owns its own value.",
        "use": [
          "A local view preference nothing else on the page reacts to."
        ],
        "dont": [
          "Anything persisted or shared. Two readers of an uncontrolled value are two readers that will disagree.",
          "Changing it later to reset the group — it is read once, at mount."
        ],
        "conflicts": [
          "value"
        ],
        "a11y": null
      },
      {
        "name": "onValueChange",
        "optional": true,
        "type": "(value: ToggleButtonGroupValue) => void",
        "description": "Called with the selection the group should move to.",
        "declared": true,
        "summary": "Called with the selection the group should move to — a value, a list in DOM order, or `null`.",
        "use": [
          "Applying the change and letting it flow back in through `value`."
        ],
        "dont": [
          "Assuming it only fires on click; in single mode the arrow keys select as they move, which is the radio pattern working correctly.",
          "Re-sorting the array it hands you — it is already in the order the user sees."
        ],
        "conflicts": [],
        "a11y": null
      },
      {
        "name": "selectionMode",
        "optional": true,
        "type": "ToggleButtonGroupSelectionMode",
        "description": "Whether this group holds one answer or any number of independent states.",
        "default": "\"single\"",
        "values": [
          "single",
          "multiple"
        ],
        "declared": true,
        "summary": "Whether this group holds one answer or any number of independent states — which also decides its ARIA pattern.",
        "use": [
          "`single` for a choice: alignment, a date range preset, a view mode.",
          "`multiple` for independent states that happen to sit together: bold, italic, underline."
        ],
        "dont": [
          "`single` for navigation between views — that is a tab list, which announces itself as one and manages its own panels.",
          "Switching it at runtime. The role, the focus model and the shape of `value` all change with it, so a mid-life switch is a different control appearing in place of the old one."
        ],
        "conflicts": [],
        "a11y": "`single` renders a `radiogroup` of `radio`s with one tab stop and arrow-key navigation; `multiple` renders a `group` of `aria-pressed` toggles, each its own tab stop."
      },
      {
        "name": "deselectable",
        "optional": true,
        "type": "boolean",
        "description": "Single mode only: whether clicking the selected option clears the selection.",
        "declared": true,
        "summary": "Single mode only: whether clicking the selected option clears the selection.",
        "use": [
          "Filters, where \"none of these\" is a real answer the user should be able to get back to."
        ],
        "dont": [
          "Settings that must always have a value — a text alignment with nothing selected is a state the document cannot actually be in.",
          "Multiple mode, where it means nothing: every item already deselects itself."
        ],
        "conflicts": [],
        "a11y": "Only clicking clears the selection. Arrow keys always move to an option and select it, because an arrow key that could empty a radio group would make the group impossible to navigate through."
      },
      {
        "name": "label",
        "optional": true,
        "type": "string",
        "description": "Accessible name for the group. Required unless `labelledBy` names an existing element.",
        "declared": true,
        "summary": "Accessible name for the group — the question these options answer.",
        "use": [
          "Always, unless `labelledBy` points at a visible label that already asks it.",
          "Naming the question, not the widget: \"Text alignment\", not \"Alignment toggle group\"."
        ],
        "dont": [
          "Leaving it off. An unnamed radiogroup is announced as a group of options with nothing to say what they choose between."
        ],
        "conflicts": [
          "labelledBy"
        ],
        "a11y": "Becomes the group's `aria-label`. In single mode this is the name a screen reader reads before \"1 of 3\", so without it the count is meaningless."
      },
      {
        "name": "labelledBy",
        "optional": true,
        "type": "string",
        "description": "Id of a visible element that already names this group.",
        "declared": true,
        "summary": "Id of a visible element that already names this group.",
        "use": [
          "A field label or heading sits above the group and says exactly what the name would."
        ],
        "dont": [
          "Pointing at an element that may not render — a dangling reference leaves the group unnamed with nothing on screen to show it."
        ],
        "conflicts": [
          "label"
        ],
        "a11y": "Wins over `label` when both are passed: it names the group with text the user can see."
      },
      {
        "name": "orientation",
        "optional": true,
        "type": "ButtonGroupOrientation",
        "description": null,
        "default": "\"horizontal\"",
        "declared": true,
        "summary": "Which way the options stack, and — in single mode — which arrows move between them.",
        "use": [
          "`vertical` in a narrow rail where a row of options would wrap."
        ],
        "dont": [
          "Assuming it constrains the keys: both axes' arrow keys move in single mode regardless, because a user who reaches for Down in a horizontal group should not hit a dead end."
        ],
        "conflicts": [],
        "a11y": "Announced as `aria-orientation` in single mode only, where arrow-key navigation actually exists to back the promise up."
      },
      {
        "name": "size",
        "optional": true,
        "type": "ToggleButtonSize",
        "description": "Control height for every option. Set here, never per option.",
        "default": "\"md\"",
        "declared": true,
        "summary": "Control height and text size for every option, from the `size.control.*` scale.",
        "use": [
          "Matching the density of the surrounding controls — set once here rather than on each option."
        ],
        "dont": [
          "Setting it per option. A set of options that measure differently is a ragged bar, which is why this prop lives on the group and not on the items."
        ],
        "conflicts": [],
        "a11y": null
      },
      {
        "name": "attached",
        "optional": true,
        "type": "boolean",
        "description": "Joins the options into one continuous bar. Defaults to true.",
        "default": "true",
        "declared": true,
        "summary": "Joins the options into one continuous bar, with shared borders and only the outer corners rounded.",
        "use": [
          "The default, and the right one for a segmented control: the options are one question, so they read as one control."
        ],
        "dont": [
          "Keeping it attached when the options wrap onto a second line — a broken bar reads as two controls."
        ],
        "conflicts": [],
        "a11y": "Visual only. The role, the selection and the focus model are the same either way."
      },
      {
        "name": "disabled",
        "optional": true,
        "type": "boolean",
        "description": "Blocks activation for every option while keeping them focusable.",
        "declared": true,
        "summary": "Blocks activation for every option while keeping them focusable, announced, and readable in their current state.",
        "use": [
          "The whole question is unavailable — a filter over data that hasn't loaded, say."
        ],
        "dont": [
          "Disabling one option; that belongs on the option itself, which stays reachable and just refuses to become the answer.",
          "Expressing \"nothing selected\" — an empty group and a disabled group are different facts."
        ],
        "conflicts": [],
        "a11y": "Emits `aria-disabled` on each option, never the native `disabled` attribute. Arrow keys still move focus through the group so its options remain discoverable."
      }
    ],
    "usage": [
      {
        "case": "Single choice — a segmented control",
        "when": "The default: one answer out of a few, all visible at once.",
        "example": "<ToggleButtonGroup\n  label=\"Text alignment\"\n  value={align}\n  onValueChange={setAlign}\n>\n  <ToggleButton value=\"left\" iconOnly aria-label=\"Align left\">⇤</ToggleButton>\n  <ToggleButton value=\"center\" iconOnly aria-label=\"Align center\">↔</ToggleButton>\n  <ToggleButton value=\"right\" iconOnly aria-label=\"Align right\">⇥</ToggleButton>\n</ToggleButtonGroup>",
        "notes": "A radiogroup: one tab stop, arrows move and select. Each icon-only option still needs its own `aria-label` — the group's name is the question, not the answers."
      },
      {
        "case": "Independent states that sit together",
        "when": "Several on/off states that are not alternatives to each other.",
        "example": "<ToggleButtonGroup\n  label=\"Text style\"\n  selectionMode=\"multiple\"\n  value={styles}\n  onValueChange={setStyles}\n>\n  <ToggleButton value=\"bold\">Bold</ToggleButton>\n  <ToggleButton value=\"italic\">Italic</ToggleButton>\n</ToggleButtonGroup>",
        "notes": "Each option keeps its own tab stop here, and the value comes back in DOM order however the user clicked into it."
      },
      {
        "case": "A filter that can be cleared",
        "when": "\"None of these\" is a real answer, so re-clicking the selected option should get back to it.",
        "example": "<ToggleButtonGroup\n  label=\"Status filter\"\n  deselectable\n  value={status}\n  onValueChange={setStatus}\n>\n  <ToggleButton value=\"open\">Open</ToggleButton>\n  <ToggleButton value=\"closed\">Closed</ToggleButton>\n</ToggleButtonGroup>",
        "notes": "Only clicking clears it. Arrow keys always land on an option and select it — an arrow that could empty the group would make it impossible to move through."
      },
      {
        "case": "Reaching for the wrong component",
        "when": "The options switch between views rather than setting a value.",
        "example": "<ToggleButtonGroup label=\"Section\" value={section} onValueChange={setSection}>\n  <ToggleButton value=\"overview\">Overview</ToggleButton>\n  <ToggleButton value=\"activity\">Activity</ToggleButton>\n</ToggleButtonGroup>",
        "notes": "Don't. Navigation between panels is a tab list: it announces itself as one, owns the relationship to the panels, and moves focus into them. This system has no Tabs yet — flag the gap rather than dressing a toggle group up as one."
      }
    ],
    "behavior": {
      "primitive": "getToggleButtonGroupProps",
      "summary": "Selection state, the ARIA pattern that state implies, and — in single mode — roving focus with arrow-key navigation. Nothing here touches the DOM: where the radio pattern requires focus to move, the primitive reports which value should receive it and the wrapper does the focusing.",
      "decisions": [
        {
          "decision": "The selection mode picks the ARIA pattern: `single` is a radiogroup, `multiple` is a group of pressed toggles",
          "why": "They are different controls wearing the same clothes. \"One of these\" is a radio group, and announcing it as one is what tells a screen-reader user how many options there are and which is chosen. \"Any of these\" is a set of independent toggles, and forcing it into radio semantics would claim the options are mutually exclusive when they are not."
        },
        {
          "decision": "Focus behaviour differs between the two modes, deliberately",
          "why": "Single mode is one tab stop with arrow keys between options, because that is the radio pattern and users of radio groups expect it. Multiple mode gives every item its own tab stop, consistent with `button-group` and with every other button in this system. The inconsistency is the ARIA patterns', not ours — matching them is what makes each mode behave the way its users already know."
        },
        {
          "decision": "Roving tabindex is derived from the value, never stored",
          "why": "In a radio group the selected option is the tab stop, and an empty group is entered at its first option. Deriving it means focus position and value cannot disagree, because there is no second piece of state to fall out of step."
        },
        {
          "decision": "Arrow keys select as they move, but a disabled option only takes focus",
          "why": "Selecting on arrow is the radio pattern. This system's disabled controls stay focusable and announced, so a disabled option must still be reachable — it just must not become the answer. Skipping it entirely would hide from a keyboard user that the option exists at all."
        },
        {
          "decision": "The selection is always reported in DOM order",
          "why": "Multiple mode rebuilds the array from the item order rather than appending to it, so what the caller stores matches what the user sees. Click order is an implementation detail of how someone got there, and persisting it produces values that shuffle between sessions for no reason."
        },
        {
          "decision": "Values are strings",
          "why": "They end up in `data-` attributes, form fields, query params and saved preferences, all of which are strings. A generic value type would push the stringification into every call site instead of settling it once here."
        },
        {
          "decision": "`aria-orientation` is announced in single mode only",
          "why": "It is a promise that arrow keys move along that axis. Single mode keeps that promise; multiple mode deliberately does not implement arrow navigation, so announcing an orientation there would advertise a behaviour that isn't present."
        },
        {
          "decision": "`size` belongs to the group; `variant` stays on the item",
          "why": "Mixed sizes break the shape — a segmented bar with items of two heights is ragged — so the group owns the decision that has to be uniform. Variant is a per-item emphasis choice that stays legible when it varies, and moving it to the group would mean two places could set it."
        }
      ]
    },
    "tokens": {
      "attached": {
        "summary": "The default, and what a segmented control is: the options are one question, so they close up into one bar and only the outer corners round.",
        "tokens": {
          "gap": "space.0",
          "inner corner border-radius": "radius.none",
          "outer corner border-radius": "radius.element, as logical corners so the bar flips in RTL",
          "collapsed shared border": "negative margin of border.1, so two adjacent borders read as one stroke",
          "focused item stacking": "raised above its neighbours so the focus ring is not clipped by the overlapping border — a stacking fix, not a token",
          "item height": "size.control.md — set once on the group and pushed to every option, because a bar of options at two heights is ragged",
          "transition-duration": "motion.interactive.duration",
          "transition-timing-function": "motion.interactive.easing",
          "outline (focus-visible)": "focus.ring-width solid theme.focus-ring, offset focus.ring-offset — owned by the option, never drawn around the group"
        }
      },
      "spaced": {
        "summary": "Options that belong together but read as separate controls — the right shape when they can wrap, since a broken bar reads as two controls.",
        "tokens": {
          "gap": "space.gap.sm",
          "item border-radius": "radius.element — each option keeps the corners it sets for itself",
          "item height": "size.control.md — set once on the group and pushed to every option, because a bar of options at two heights is ragged",
          "transition-duration": "motion.interactive.duration",
          "transition-timing-function": "motion.interactive.easing",
          "outline (focus-visible)": "focus.ring-width solid theme.focus-ring, offset focus.ring-offset — owned by the option, never drawn around the group"
        }
      }
    },
    "example": "<ToggleButtonGroup\n      label=\"Status filter\"\n      deselectable\n      value={status}\n      onValueChange={(next) => setStatus(next as string | null)}\n    >\n      <ToggleButton value=\"open\">Open</ToggleButton>\n      <ToggleButton value=\"closed\">Closed</ToggleButton>\n    </ToggleButtonGroup>"
  }
]
