{
  "name": "badge",
  "title": "Badge",
  "family": "content",
  "description": "A small inline label for a status, a count, or a category.",
  "status": {
    "css": {
      "state": "future"
    },
    "react": {
      "state": "future"
    },
    "figma": {
      "state": "future"
    }
  },
  "dependencies": [],
  "files": [],
  "tier": "free",
  "props": {
    "children": {
      "type": "ReactNode",
      "required": true,
      "summary": "The badge's text.",
      "dont": [
        "A sentence. A badge that wraps is a Notice wearing the wrong component."
      ]
    },
    "variant": {
      "type": "\"neutral\" | \"accent\" | \"success\" | \"warning\" | \"danger\"",
      "default": "\"neutral\"",
      "summary": "Which role's tone the badge wears.",
      "use": [
        "`neutral` for a count or a category — the default, and right whenever the badge is not reporting health.",
        "`success` / `warning` / `danger` only for actual status, where the colour is telling the reader something the text also says."
      ],
      "dont": [
        "Colour as decoration. Five badges in five colours that mean nothing teaches readers to ignore the colour on the one that does.",
        "`danger` for 'important'. It means something is wrong."
      ],
      "a11y": "Every variant uses its role's subtle background with that role's foreground — never the saturated `bg`, which is a non-text indicator tone. The text carries the meaning; colour only reinforces it, so a badge is never colour-only."
    },
    "dot": {
      "type": "boolean",
      "summary": "Renders a status dot before the text.",
      "use": [
        "A status badge in a dense table, where the dot is findable at a glance and the text confirms it."
      ],
      "dont": [
        "A dot with no text. A bare coloured dot is colour-only information, which is exactly what the variant contract forbids."
      ],
      "a11y": "Decorative and `aria-hidden` — it repeats what the text already says."
    }
  },
  "usage": [
    {
      "case": "A count beside a label",
      "when": "The commonest case: a number qualifying something next to it.",
      "example": "<Button>\n  Messages\n  <Badge>3</Badge>\n</Button>",
      "notes": "Neutral by default: a count is not a status, and colouring it would imply something about the number."
    },
    {
      "case": "A status in a table row",
      "when": "Reporting health, where the colour reinforces text that already says it.",
      "example": "<Badge variant=\"danger\" dot>Failed</Badge>",
      "notes": "The dot is decorative and aria-hidden. Removing it would lose nothing for a screen reader, which is the test of whether a visual flourish is safe."
    },
    {
      "case": "Reaching for the wrong component",
      "when": "The text is a sentence explaining something.",
      "example": "<Badge variant=\"warning\">Your trial ends in three days, renew to keep your data</Badge>",
      "notes": "Don't. That is a Notice: it has room for the sentence, a heading, and somewhere to put the action. A badge that wraps onto two lines has already stopped being a badge."
    }
  ]
}
