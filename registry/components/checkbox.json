{
  "name": "checkbox",
  "title": "Checkbox",
  "family": "inputs",
  "description": "A single on/off choice, with an indeterminate state for a parent summarising its children.",
  "status": {
    "css": {
      "state": "future"
    },
    "react": {
      "state": "future"
    },
    "figma": {
      "state": "future"
    }
  },
  "dependencies": [
    "state-layer"
  ],
  "files": [],
  "tier": "free",
  "behavior": {
    "primitive": "getCheckboxProps",
    "summary": "Checked state, the indeterminate third appearance, and the activation guard. Built on a native input[type=checkbox] so keyboard, form participation and the checked semantics come from the platform rather than being re-implemented.",
    "decisions": [
      {
        "decision": "A native input, not a div with role=\"checkbox\"",
        "why": "Space to toggle, participation in a form's data, and the checked state itself are all free from the platform and all easy to get subtly wrong by hand. The visual is drawn by a sibling element; the input stays in the DOM and keeps the semantics."
      },
      {
        "decision": "`indeterminate` is a separate prop, not a third value of `checked`",
        "why": "It answers a different question. `checked` is this control's own value; `indeterminate` says the control is standing in for a set of children that disagree. A tri-state `checked` would force every caller to handle a value that only a parent checkbox can ever have, and would make the common two-state case read as incomplete."
      },
      {
        "decision": "Indeterminate is set as a DOM property, never an attribute",
        "why": "There is no `indeterminate` content attribute in HTML — only an IDL property — so it cannot be expressed in JSX and has to be assigned to the element. The wrapper owns that, which is the whole reason this component cannot be a bare styled input."
      },
      {
        "decision": "Activation is guarded, not disabled",
        "why": "This system's disabled controls stay focusable and announced. `readOnly` is the input analogue used by text-field, but it has no effect on a checkbox, so the guard here is a refused activation plus aria-disabled — the same shape as the button primitive."
      },
      {
        "decision": "The label is required and is a real `<label for>`",
        "why": "A checkbox's hit target should include its text, and a bound label gives that for free. It also means the accessible name cannot drift from what is on screen."
      }
    ]
  },
  "props": {
    "label": {
      "type": "ReactNode",
      "required": true,
      "summary": "The checkbox's label. Always required.",
      "use": [
        "Always. A checkbox with no label is a control nobody can describe."
      ],
      "dont": [
        "Using the surrounding sentence as the label and leaving this empty — the binding is what makes the text a hit target and an accessible name."
      ],
      "a11y": "Rendered as a real `<label for>` bound to the input, so clicking the text toggles the box."
    },
    "checked": {
      "type": "boolean",
      "summary": "Whether the box is checked. Makes the component controlled.",
      "use": [
        "Whenever the value lives outside the component, which is most of the time."
      ],
      "dont": [
        "Passing it without `onCheckedChange`; the box would render its state and then refuse to change it."
      ],
      "conflicts": [
        "defaultChecked"
      ],
      "a11y": "Drives the native checked state, which is what assistive technology reports."
    },
    "defaultChecked": {
      "type": "boolean",
      "summary": "Starting state for an uncontrolled checkbox.",
      "use": [
        "Genuinely local state nothing else reads."
      ],
      "conflicts": [
        "checked"
      ]
    },
    "onCheckedChange": {
      "type": "(checked: boolean, event: ChangeEvent<HTMLInputElement>) => void",
      "summary": "Called with the state the box should move to, not the one it is leaving.",
      "dont": [
        "Reading `event.target.checked` instead of the argument — they agree, but the argument is the one this component promises."
      ]
    },
    "indeterminate": {
      "type": "boolean",
      "summary": "Shows the box as neither checked nor unchecked: a parent summarising children that disagree.",
      "use": [
        "A 'select all' box above a list where only some rows are selected."
      ],
      "dont": [
        "Expressing 'unknown' or 'not yet answered'. It means partially checked, and a screen reader announces it as `mixed`.",
        "Combining it with a `checked` that is also true and expecting both to show — indeterminate wins visually and in what is announced, which is why they are not one prop."
      ],
      "a11y": "Announced as `aria-checked=\"mixed\"`. Set as a DOM property by this component, because HTML has no such attribute."
    },
    "description": {
      "type": "ReactNode",
      "summary": "Helper text under the label.",
      "dont": [
        "Error text. A checkbox that is invalid belongs in a field wrapper that owns the message."
      ]
    },
    "disabled": {
      "type": "boolean",
      "summary": "Blocks activation while keeping the box focusable and announced.",
      "a11y": "Emits `aria-disabled` and refuses activation. Never the native `disabled` attribute, which would remove it from the tab order."
    },
    "required": {
      "type": "boolean",
      "summary": "Marks the checkbox as required to assistive technology and in the label.",
      "a11y": "Emits `aria-required`. The visible marker is the label's job."
    },
    "value": {
      "type": "string",
      "summary": "Submitted value when the box is checked and inside a form."
    }
  },
  "usage": [
    {
      "case": "A single consent box",
      "when": "The commonest case: one box, one label, its own state.",
      "example": "<Checkbox\n  label=\"Email me product updates\"\n  checked={subscribed}\n  onCheckedChange={setSubscribed}\n/>"
    },
    {
      "case": "A parent summarising a list",
      "when": "Some rows selected, so the parent is neither checked nor unchecked.",
      "example": "<Checkbox\n  label=\"Select all\"\n  checked={all}\n  indeterminate={some && !all}\n  onCheckedChange={toggleAll}\n/>",
      "notes": "`indeterminate` is what a screen reader hears as `mixed`. It is deliberately separate from `checked`, because it answers a different question: not this box's value, but whether the children below agree."
    },
    {
      "case": "Unavailable but still readable",
      "when": "A choice that cannot be made yet, whose current state still matters.",
      "example": "<Checkbox label=\"Ship to billing address\" checked disabled />",
      "notes": "aria-disabled and a refused activation, never the native `disabled` attribute — the box stays focusable so a keyboard user can find it and read its state."
    }
  ]
}
