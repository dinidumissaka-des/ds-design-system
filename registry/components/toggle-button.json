{
  "name": "toggle-button",
  "title": "Toggle Button",
  "family": "buttons",
  "description": "A button that stays on: an on/off state the user sets directly, announced with aria-pressed.",
  "status": {
    "css": {
      "state": "future"
    },
    "react": {
      "state": "future"
    },
    "figma": {
      "state": "future"
    }
  },
  "dependencies": ["button", "state-layer"],
  "files": [],
  "tier": "free",
  "behavior": {
    "primitive": "getToggleButtonProps",
    "summary": "Pressed-state semantics over the button's own activation contract. State is not held here: the caller owns `pressed` and is told what it should become, which is what lets a toggle inside a group and a toggle on its own be the same primitive.",
    "decisions": [
      {
        "decision": "Composes `getButtonProps` instead of reimplementing activation",
        "why": "A toggle is a button that carries a state. Implementing the disabled and loading guards a second time is how the two drift apart — one gains a fix the other doesn't, and the difference only shows up in whichever one nobody tested."
      },
      {
        "decision": "`aria-pressed` is emitted in both states, never omitted when false",
        "why": "Unlike `aria-disabled`, which is absent when it doesn't apply, a missing `aria-pressed` makes the control announce as an ordinary button — precisely when the user most needs to know it is a toggle that is currently off."
      },
      {
        "decision": "`aria-pressed`, not `role=\"switch\"`",
        "why": "A switch is an on/off setting that takes effect immediately and reads as a thing with a state (\"Notifications, on\"). A toggle button is an action you leave engaged — bold, mute, pin — and reads as a verb. When the control is a setting in a form, the answer is a switch, and this component is the wrong one."
      },
      {
        "decision": "The label does not change with the state",
        "why": "`aria-pressed` already says whether it is on. Swapping the label to \"Unmute\" as well means the name and the state contradict each other — a screen reader announces \"Unmute, pressed\", which is a double negative the user has to unpick. The visual treatment carries the state; the word stays put."
      },
      {
        "decision": "No `data-pressed` attribute",
        "why": "`[aria-pressed=\"true\"]` is already a CSS selector. A parallel data attribute would be a second copy of the same fact, free to go stale."
      },
      {
        "decision": "Controlled by default, with an uncontrolled escape hatch",
        "why": "A toggle almost always reflects state that lives somewhere else — a document's formatting, a saved preference — so the controlled form is the honest default. `defaultPressed` exists for the genuinely local case, and passing both is a conflict rather than a precedence rule to memorize."
      }
    ]
  },
  "props": {
    "pressed": {
      "type": "boolean",
      "summary": "Whether the toggle is currently on. Makes the component controlled.",
      "use": [
        "Whenever the state lives outside the component — which is nearly always, since a toggle usually reflects something a document or a preference already knows."
      ],
      "dont": [
        "Passing it without `onPressedChange`; the toggle would render its state and then refuse to ever change it.",
        "Deriving it from the last click rather than from the thing being toggled — if the underlying change fails, the button is left lying about it."
      ],
      "conflicts": ["defaultPressed"],
      "a11y": "Becomes `aria-pressed`, emitted in both states. This is the only thing that tells assistive technology the control is a toggle at all."
    },
    "defaultPressed": {
      "type": "boolean",
      "summary": "Starting state for an uncontrolled toggle that owns its own value.",
      "use": [
        "Genuinely local state nothing else reads — a disclosure of extra columns in one table, say."
      ],
      "dont": [
        "Anything the rest of the page reacts to. The moment two places need the value, uncontrolled means they disagree.",
        "Resetting the toggle by changing this later — it is read once, at mount."
      ],
      "conflicts": ["pressed"],
      "a11y": "No difference in what is announced: `aria-pressed` is emitted either way."
    },
    "onPressedChange": {
      "type": "(pressed: boolean, event: MouseEvent) => void",
      "summary": "Called with the state the toggle should move to — not the one it is leaving.",
      "use": [
        "Applying the change, then letting the new state flow back in through `pressed`."
      ],
      "dont": [
        "Reading the current value off the event target; the argument already says where it is going.",
        "Treating it as a plain click handler — it does not fire when the toggle is disabled or loading."
      ],
      "a11y": "Fires for keyboard activation too, since it hangs off the same click guard the button uses. Space and Enter behave as they do on any native button."
    },
    "variant": {
      "type": "\"secondary\" | \"tertiary\"",
      "default": "\"secondary\"",
      "summary": "How loud the toggle is in its off state.",
      "use": [
        "`tertiary` for a toolbar of many toggles, where a border each would be visual noise.",
        "`secondary` for one or two toggles that need to read as controls on their own."
      ],
      "dont": [
        "Expecting a `primary` or `destructive` toggle. A filled accent control reads as \"the action to take\", which fights with \"a state you are in\", and a destructive thing you can leave switched on is a bug waiting to happen."
      ],
      "a11y": "Visual only. The pressed state must never be carried by colour alone — the on state changes fill *and* is announced through `aria-pressed`."
    },
    "size": {
      "type": "\"sm\" | \"md\" | \"lg\"",
      "default": "\"md\"",
      "summary": "Control height and text size, from the `size.control.*` scale.",
      "use": [
        "Matching the density of the surrounding controls, exactly as `Button` does."
      ],
      "dont": [
        "Mixing sizes within one group of toggles — they read as a set, so they measure as a set."
      ]
    },
    "disabled": {
      "type": "boolean",
      "summary": "Blocks activation while keeping the toggle focusable, announced, and readable in its current state.",
      "use": [
        "A toggle that cannot change yet but whose current state still matters to see."
      ],
      "dont": [
        "Expressing \"off\". Disabled and unpressed are different facts, and a disabled toggle can be either."
      ],
      "conflicts": ["loading"],
      "a11y": "Emits `aria-disabled`, never the native `disabled` attribute: the control stays in the tab order and keeps announcing its pressed state."
    },
    "loading": {
      "type": "boolean",
      "summary": "Shows a spinner and refuses activation while the change this toggle asked for is still in flight.",
      "use": [
        "A toggle whose effect is a server round trip — starring, subscribing, publishing — where the state should not settle until the change lands."
      ],
      "dont": [
        "Flipping `pressed` optimistically *and* setting `loading`; pick one story about what the button currently means.",
        "Pairing it with `disabled` — `loading` already blocks activation, and the two together say the same thing twice."
      ],
      "conflicts": ["disabled"],
      "a11y": "Emits `aria-busy` alongside `aria-disabled`, so the control is heard as busy rather than as broken. `aria-pressed` keeps reporting the state it is still in."
    },
    "iconOnly": {
      "type": "boolean",
      "summary": "Square toggle carrying an icon and no visible text.",
      "use": [
        "Toolbars where the icons are conventional and the row would otherwise be unreadably wide — bold, italic, alignment."
      ],
      "dont": [
        "Icons whose meaning is not already settled. An unlabelled toggle with an ambiguous glyph is a control nobody can describe to a colleague.",
        "Relying on the icon alone to show the pressed state — the fill must change too."
      ],
      "a11y": "You MUST pass `aria-label`. Nothing supplies an accessible name when there is no visible text, and the name must stay constant across states — the state is `aria-pressed`'s job, not the label's."
    }
  },
  "usage": [
    {
      "case": "A formatting toggle in a toolbar",
      "when": "The canonical case: the state belongs to the document, so the toggle is controlled.",
      "example": "<ToggleButton\n  variant=\"tertiary\"\n  iconOnly\n  aria-label=\"Bold\"\n  pressed={isBold}\n  onPressedChange={setBold}\n>\n  B\n</ToggleButton>",
      "notes": "The label stays \"Bold\" in both states. `aria-pressed` says which state it is in — renaming it to \"Unbold\" would make the announcement contradict itself."
    },
    {
      "case": "A toggle whose change has to reach a server",
      "when": "Starring, subscribing, publishing — anything where the state should not settle until the write lands.",
      "example": "<ToggleButton pressed={starred} loading={saving} onPressedChange={star}>\n  Star\n</ToggleButton>",
      "notes": "`loading` blocks repeat activation on its own — don't also pass `disabled`. If the write fails, leave `pressed` where it was rather than flipping it back, which reads as the user having done something."
    },
    {
      "case": "Local-only state nothing else reads",
      "when": "The rare uncontrolled case: one view's own disclosure of extra detail.",
      "example": "<ToggleButton defaultPressed onPressedChange={setShowDetail}>\n  Show detail\n</ToggleButton>",
      "notes": "The moment a second place needs to know whether detail is showing, this becomes `pressed` — uncontrolled state that two components read is state that will disagree."
    },
    {
      "case": "Reaching for the wrong component",
      "when": "The control is a setting in a form, applied immediately, and reads as a thing with a state.",
      "example": "<ToggleButton pressed={notifications} onPressedChange={setNotifications}>\n  Notifications\n</ToggleButton>",
      "notes": "Don't. That is a switch, not a toggle button: it is a setting, not an action left engaged. This system has no Switch yet — say so and flag the gap rather than dressing a toggle button up as one."
    }
  ]
}
